<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://incml.github.io/huijiang/feed.xml" rel="self" type="application/atom+xml"/><link href="https://incml.github.io/huijiang/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-09T18:21:56+00:00</updated><id>https://incml.github.io/huijiang/feed.xml</id><title type="html">blank</title><subtitle>Prof. Hui Jiang&apos;s personal website. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">The Bitcoin Lightning Network</title><link href="https://incml.github.io/huijiang/blog/2026/Lightning-Network/" rel="alternate" type="text/html" title="The Bitcoin Lightning Network"/><published>2026-03-07T00:00:00+00:00</published><updated>2026-03-07T00:00:00+00:00</updated><id>https://incml.github.io/huijiang/blog/2026/Lightning-Network</id><content type="html" xml:base="https://incml.github.io/huijiang/blog/2026/Lightning-Network/"><![CDATA[<blockquote> <p><strong>Abstract</strong></p> <p>Bitcoin’s base layer is optimized for decentralization, auditability, and adversarial robustness, not for high-throughput retail payments. The Lightning Network addresses this limitation by introducing a second-layer protocol built from bidirectional payment channels, revocable commitment states, hashed timelock contracts, and on-chain dispute resolution. This article presents a technical but accessible introduction to Lightning, starting from Bitcoin transaction primitives and building upward to channel construction, state updates, watchtowers, revocation, HTLC routing.</p> </blockquote> <h2 id="1-introduction-why-bitcoin-needs-a-payment-layer">1. Introduction: Why Bitcoin Needs a Payment Layer</h2> <p>Bitcoin is exceptionally strong as a settlement system. Its design prioritizes consensus safety, resistance to censorship, verifiability, and the ability for ordinary users to audit the ledger. Those strengths come with well-known trade-offs. Blocks arrive roughly every ten minutes, throughput is limited, and every fully validated transaction must eventually be processed by the entire network. That trade-off is acceptable for high-value settlement, but it is awkward for small, frequent, interactive payments. Buying coffee, streaming micropayments per second, or routing tiny global transfers through the base chain alone would be slow and expensive.</p> <p>The Lightning Network was developed to solve this problem without changing Bitcoin’s role as the final source of truth. The basic idea is elegant: instead of recording every payment on-chain, two users lock funds into a shared Bitcoin output, then privately update who owns what. Only the opening and closing of that relationship require on-chain transactions under normal operation. Everything in between is handled off-chain, but remains enforceable through Bitcoin scripts and signatures.</p> <p>Lightning is therefore not a separate currency, not a sidechain, and not a custodial overlay. It is a contract-based payment system that uses Bitcoin as its court of final appeal. At a high level, Lightning is a network of payment channels. A channel connects two peers, allowing them to rebalance ownership of locked funds many times without repeatedly touching the blockchain.</p> <p>If Alice and Bob share a channel, they can pay each other directly. If Alice does not share a direct channel with Bob, but Alice has a channel with Carol and Carol has one with Dave and Dave has one with Bob, then Alice can still pay Bob through a chain of conditional payments:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Alice → Carol → Dave → Bob
</code></pre></div></div> <p>This gives Lightning two scaling properties at once. First, it compresses many bilateral updates into a small number of on-chain transactions. Second, it allows a network of local channels to approximate global reach through routing.</p> <p>The protocol achieves this while preserving several strong goals:</p> <ul> <li>either channel party can unilaterally exit to Bitcoin main chain</li> <li>outdated states are punishable</li> <li>routed payments are atomic</li> <li>intermediaries do not need custody over user funds</li> <li>final settlement remains in BTC on Bitcoin main chain</li> </ul> <p>Those guarantees come from a combination of multisignature funding outputs, commitment transactions, revocation keys, timelocks, and HTLCs.</p> <hr/> <h2 id="2-the-lightning-protocol">2. The Lightning Protocol</h2> <h3 id="21-payment-channels-as-off-chain-shared-ledgers">2.1 Payment Channels as Off-Chain Shared Ledgers</h3> <p>A Lightning channel begins with an on-chain funding transaction. Suppose Alice and Bob each contribute 1 BTC. The total channel capacity is therefore 2 BTC.</p> <p>Economically, it is useful to imagine the channel as a tiny private ledger:</p> <ul> <li>Alice initially owns 1.0 BTC of the 2.0 BTC channel</li> <li>Bob initially owns 1.0 BTC of the 2.0 BTC channel</li> </ul> <p>But that split is not represented directly on-chain. On-chain, there is only one shared output. The internal ownership split is represented by off-chain <strong>commitment transactions</strong>.</p> <p>This distinction is central to Lightning:</p> <ul> <li><strong>on-chain reality:</strong> one shared UTXO</li> <li><strong>off-chain reality:</strong> a sequence of signed balance states</li> </ul> <p>The entire protocol is about safely moving between those off-chain states.</p> <h3 id="22-funding-the-channel-2-of-2-multisig-and-p2wsh">2.2 Funding the Channel: 2-of-2 Multisig and P2WSH</h3> <p>At first, Alice and Bob need to open a lightning channel cooperatively by exchanging their public keys. The funding output must ensure that neither Alice nor Bob can spend the channel alone. The simplest construction is a 2-of-2 multisignature script:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OP_2 &lt;Alice_pubkey&gt; &lt;Bob_pubkey&gt; OP_2 OP_CHECKMULTISIG
</code></pre></div></div> <p>In Lightning, this script is typically committed via <strong>P2WSH</strong> (Pay-to-Witness-Script-Hash). The full witness script is hashed with SHA256, and the resulting output script is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OP_0 &lt;32-byte SHA256(script)&gt;
</code></pre></div></div> <p>Alice and Bob need each contribute 1 BTC to the P2WSH address, thereby funding the channel with a total of 2 BTC.</p> <p>This construction gives Lightning two crucial properties:</p> <ol> <li>the channel capacity is secured by Bitcoin consensus</li> <li>the channel can only move if the spending conditions encoded by the protocol are satisfied</li> </ol> <h4 id="p2wsh-in-context">P2WSH in Context</h4> <p>Because Lightning relies heavily on P2WSH, it is worth restating how it works. A P2WSH output commits to the SHA256 hash of a witness script. The actual script is not revealed until spend time. This is particularly useful for Lightning because commitment and HTLC scripts can be large and complex. By storing only the script hash on-chain at funding time, the protocol keeps opening transactions compact. It is also useful to distinguish P2WSH from P2WPKH. Both are version-0 SegWit outputs and both may begin with <code class="language-plaintext highlighter-rouge">bc1q...</code> as human-readable addresses, but on-chain they are distinguishable by witness program length:</p> <ul> <li><code class="language-plaintext highlighter-rouge">0014 &lt;20 bytes&gt;</code> means P2WPKH</li> <li><code class="language-plaintext highlighter-rouge">0020 &lt;32 bytes&gt;</code> means P2WSH</li> </ul> <h3 id="23-commitment-transactions-the-heart-of-lightning">2.3 Commitment Transactions: The Heart of Lightning</h3> <p>Once the funding output exists, Alice and Bob need a way to represent who owns how much of it at any given moment. This is done with commitment transactions.</p> <p>A commitment transaction spends the funding output and redistributes the funds according to the latest agreed channel state. Importantly, there are always <strong>two</strong> commitment transactions:</p> <ul> <li>Alice’s commitment transaction</li> <li>Bob’s commitment transaction</li> </ul> <p>These are not identical byte-for-byte. They represent the same balances, but from different perspectives.</p> <p>For example, if the current channel state is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Alice = 1.0 BTC
Bob   = 1.0 BTC
</code></pre></div></div> <p>then Alice’s version might pay:</p> <ul> <li><code class="language-plaintext highlighter-rouge">to_local</code> → Alice, delayed</li> <li><code class="language-plaintext highlighter-rouge">to_remote</code> → Bob, immediate</li> </ul> <p>while Bob’s version pays:</p> <ul> <li><code class="language-plaintext highlighter-rouge">to_local</code> → Bob, delayed</li> <li><code class="language-plaintext highlighter-rouge">to_remote</code> → Alice, immediate</li> </ul> <p>The names are relative. “Local” means “me, the owner of this commitment transaction.” “Remote” means “the counterparty.”</p> <h4 id="to_local-and-to_remote-outputs"><code class="language-plaintext highlighter-rouge">to_local</code> and <code class="language-plaintext highlighter-rouge">to_remote</code> Outputs</h4> <p>The <code class="language-plaintext highlighter-rouge">to_remote</code> output is conceptually simple. It usually pays the counterparty in a straightforward SegWit form, often P2WPKH, and is immediately spendable.</p> <p>The <code class="language-plaintext highlighter-rouge">to_local</code> output is the interesting one. It includes both a normal delayed withdrawal path and an immediate revocation path. The redeem condition for <code class="language-plaintext highlighter-rouge">to_local</code> output is typically designed as two deperate paths:</p> <ul> <li><strong>revocation path</strong>: the counterparty may spend immediately if they can provide a valid signature under the revocation key. The signature requires the revocation secret that is not available before revocation.</li> <li><strong>delayed local path</strong>: the owner of that commitment may spend, but only with their own key and only after waiting the required delay (144-block delay).</li> </ul> <p>That waiting window is what gives the honest counterparty time to detect and punish a revoked-state broadcast (discuss later).</p> <p>For example, the logic is roughly:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>IF revocation is used, check signature under revocation key
ELSE enforce delay, then check signature under delayed key
</code></pre></div></div> <h4 id="relative-timelocks-and-the-144-block-delay">Relative Timelocks and the 144-Block Delay</h4> <p>Lightning primarily uses <strong>relative timelocks</strong> for <code class="language-plaintext highlighter-rouge">to_local</code> outputs. These are implemented using <code class="language-plaintext highlighter-rouge">OP_CHECKSEQUENCEVERIFY</code> (CSV) together with the spending input’s <code class="language-plaintext highlighter-rouge">nSequence</code>. A common tutorial value is 144 blocks, roughly one day. The key idea is that the owner of a <code class="language-plaintext highlighter-rouge">to_local</code> output cannot spend it immediately. They must wait relative to the confirmation height of the commitment transaction. If the commitment confirms at block 900000 and the delay is 144, then the earliest block where the delayed spend becomes valid is approximately 900144.</p> <p>The enforcement is split across script and consensus:</p> <ul> <li>the script requires a minimum delay via <code class="language-plaintext highlighter-rouge">OP_CHECKSEQUENCEVERIFY</code></li> <li>Bitcoin nodes interpret the spending input’s <code class="language-plaintext highlighter-rouge">nSequence</code> according to BIP68 and verify that enough blocks have elapsed</li> </ul> <p>This may look slightly redundant, but it is a deliberate design: the script expresses the contract requirement, while the transaction metadata carries the declared relative locktime that nodes can enforce.</p> <h4 id="why-there-are-two-asymmetric-commitment-transactions-instead-of-one">Why There Are Two Asymmetric Commitment Transactions Instead of One</h4> <p>A naïve design might try to use one shared transaction for both parties. That would fail to give each side an immediately usable unilateral exit and would make punishment logic ambiguous.</p> <p>Lightning instead arranges that each party always holds a fully signed transaction they can broadcast on their own. To achieve that, each party signs the other party’s commitment transaction. In practical terms:</p> <ul> <li>Bob signs Alice’s commitment transaction</li> <li>Alice signs Bob’s commitment transaction</li> </ul> <p>So at any moment:</p> <ul> <li>Alice holds a valid transaction she can broadcast if Bob disappears</li> <li>Bob holds a valid transaction he can broadcast if Alice disappears</li> </ul> <p>This design allows unilateral closes without cooperation, while still making old states punishable (discuss later).</p> <hr/> <h2 id="3-a-channel-example-to-demenstrate-technical-detals">3. A Channel Example to Demenstrate Technical Detals</h2> <p>Consider a simple channel between Alice and Bob:</p> <ul> <li>Alice contributes 1 BTC</li> <li>Bob contributes 1 BTC</li> <li>total channel capacity = 2 BTC</li> </ul> <p>Initial state:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>S0:
Alice = 1.0
Bob   = 1.0
</code></pre></div></div> <p>Now imagine three off-chain payments:</p> <ol> <li>Alice pays Bob 0.2 BTC</li> <li>Alice pays Bob 0.1 BTC</li> <li>Bob pays Alice 0.5 BTC</li> </ol> <p>The resulting state sequence is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>S0: Alice 1.0, Bob 1.0
S1: Alice 0.8, Bob 1.2
S2: Alice 0.7, Bob 1.3
S3: Alice 1.2, Bob 0.8
</code></pre></div></div> <p>Throughout this entire sequence, the blockchain still sees only the funding output. None of these intermediate balance changes are published on-chain. They exist only as evolving commitment transactions and revocation records held by Alice and Bob.</p> <p>If they later cooperate to close the channel, they can produce a simple closing transaction on the Bitcoin chain paying:</p> <ul> <li>1.2 BTC to Alice’s chosen address</li> <li>0.8 BTC to Bob’s chosen address</li> </ul> <h3 id="31-how-to-open-and-fund-the-channel-set-up-s0">3.1 How to Open and Fund the Channel (Set up S0)</h3> <p>Alice and Bob follow a typical protocol flow to work cooperatively to send funds to the channel’s locked address and simultaneously set the channel state as <code class="language-plaintext highlighter-rouge">S0</code>. Assume Alice initiates the process.</p> <p><strong>Step 1</strong>: Alice sends <code class="language-plaintext highlighter-rouge">open_channel</code></p> <p><strong>Step 2</strong>: Bob replies <code class="language-plaintext highlighter-rouge">accept_channel</code></p> <p><strong>Step 3</strong>: Both exchange inputs and pubkeys</p> <p><strong>Step 4</strong>: Both build the funding transaction that has two inputs and one output, without signatures at this stage</p> <blockquote> <p><strong>Inputs</strong>:</p> <ul> <li>Alice UTXO (1 BTC)</li> <li>Bob UTXO (1 BTC)</li> </ul> <p><strong>Output</strong>:</p> <ul> <li>2 BTC → P2WSH multisig (channel)</li> </ul> </blockquote> <p><strong>Step 5</strong>: Compute <em>txid</em> for the above funding transaction. (It is possible to do so in SegWit without using inputs’ signatures)</p> <p><strong>Step 6</strong>: Construct two asymmetric commitment transactions referencing funding_txid</p> <blockquote> <ul> <li> <p>Alice commitment transaction: <code class="language-plaintext highlighter-rouge">S0_A</code></p> <p><strong>Input</strong>:</p> <ul> <li>P2WSH funding_output (2 BTC)</li> </ul> <p><strong>Outputs</strong>:</p> <ul> <li>to_local: 1 BTC → Alice (delayed)</li> <li>to_remote: 1 BTC → Bob (immediate)</li> </ul> </li> </ul> </blockquote> <p>The <code class="language-plaintext highlighter-rouge">to_local</code> output usually includes a revocation clause that allows the counterparty (Bob) to seize all funds in the channel if Bob can provides a valid signature under <code class="language-plaintext highlighter-rouge">&lt;revocation_pubkey&gt;</code>, which is calculated for <code class="language-plaintext highlighter-rouge">S0_A</code>. Bob does not have the secret key to generate the valid signature at this stage.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OP_IF
    &lt;revocation_pubkey&gt;
OP_ELSE
    144 OP_CHECKSEQUENCEVERIFY
    OP_DROP
    &lt;Alice_delayed_pubkey&gt;
OP_ENDIF
OP_CHECKSIG
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">to_remote</code> output uses Bob’s P2WPKH address, where Bob can spend it immediately</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OP_0 HASH160(Bob_pubkey)
</code></pre></div></div> <blockquote> <ul> <li>Bob’s commitment transaction: <code class="language-plaintext highlighter-rouge">S0_B</code></li> </ul> <p><strong>Input</strong>:</p> <ul> <li>P2WSH funding_output (2 BTC)</li> </ul> <p><strong>Outputs</strong>:</p> <ul> <li>to_local: 1 BTC → Bob (delayed)</li> <li>to_remote: 1 BTC → Alice (immediate)</li> </ul> </blockquote> <p>Similarly, the <code class="language-plaintext highlighter-rouge">to_local</code> also includes a revocation clause that allows Alice to seize all funds if she can provide a valid signature. The <code class="language-plaintext highlighter-rouge">to_remote</code> output goes to Alice’s P2WPKH address.</p> <p><strong>Step 6</strong>: Sign two commitment transactions first and exchange signatures and both verify</p> <p><strong>Step 7</strong>: Both sign their own inputs in the funding transaction and exchange signatures, both verify</p> <p><strong>Step 8</strong>: Broadcast the funding transaction onto the Bitcoin network. After this funding transaction is confirmed, the channel is in state <code class="language-plaintext highlighter-rouge">S0</code>.</p> <h3 id="32-the-commitment-update-protocol-s0--s1">3.2 The Commitment Update Protocol (S0 → S1)</h3> <p>When Alice and Bob proceed from state <code class="language-plaintext highlighter-rouge">S0</code> to <code class="language-plaintext highlighter-rouge">S1</code>, the protocol must guarantee that no party can exploit a race condition. A simplified update flow works as follows:</p> <p><strong>Step 1</strong>: The sender (Alice) proposes the new balance state: <code class="language-plaintext highlighter-rouge">S1: Alice 0.8, Bob 1.2</code>, and sends Bob a message <code class="language-plaintext highlighter-rouge">update balance</code></p> <p><strong>Step 2</strong>: Both sides build new commitment transactions:</p> <blockquote> <ul> <li> <p>Alice commitment transaction: <code class="language-plaintext highlighter-rouge">S1_A</code></p> <p><strong>Input</strong>:</p> <ul> <li>P2WSH funding_output (2 BTC)</li> </ul> <p><strong>Outputs</strong>:</p> <ul> <li>to_local: 0.8 BTC → Alice (delayed)</li> <li>to_remote: 1.2 BTC → Bob ( immediate)</li> </ul> </li> </ul> </blockquote> <blockquote> <ul> <li> <p>Bob commitment transaction: <code class="language-plaintext highlighter-rouge">S1_B</code></p> <p><strong>Input</strong>:</p> <ul> <li>P2WSH funding_output (2 BTC)</li> </ul> <p><strong>Outputs</strong>:</p> <ul> <li>to_local: 1.2 BTC → Bob (delayed)</li> <li>to_remote: 0.8 BTC → Alice ( immediate)</li> </ul> </li> </ul> </blockquote> <p>The <code class="language-plaintext highlighter-rouge">to_local</code> outputs includes new revocation clauses that allows the counterparty to seize all funds under new revocation pubkeys, which are calculated for <code class="language-plaintext highlighter-rouge">S1</code>.</p> <p><strong>Step 3</strong>: The sender (Alice) signs the receiver’s (Bob) new commitment transaction <code class="language-plaintext highlighter-rouge">S1_B</code>, and send Bob the signed comitment transaction <code class="language-plaintext highlighter-rouge">S1_B_signed</code>.</p> <p><strong>Step 4</strong>: The receiver (Bob) revokes the old state <code class="language-plaintext highlighter-rouge">S0</code> by releasing the revocation secret of the last commitement transaction <code class="language-plaintext highlighter-rouge">S0_B</code>, which allows Alice to construct a valid revocation signature in <code class="language-plaintext highlighter-rouge">S0_B</code>. (From now on, if Bob broadcasts <code class="language-plaintext highlighter-rouge">S0_B</code>, Alice can seize entire channel.)</p> <p><strong>Step 5</strong>: The receiver (Bob) signs the sender’s (Alice) new commitment transaction <code class="language-plaintext highlighter-rouge">S1_A</code>, and send Alice the signed commitment tx <code class="language-plaintext highlighter-rouge">S1_A_signed</code>.</p> <p><strong>Step 6</strong>: The sender (Alice) revokes the old state <code class="language-plaintext highlighter-rouge">S0</code> by releasing the revocation secret of the last commitement transaction <code class="language-plaintext highlighter-rouge">S0_A</code>, which allows Bob to construct a valid revocation signature in <code class="language-plaintext highlighter-rouge">S0_A</code>. (From now on, if Alice broadcasts <code class="language-plaintext highlighter-rouge">S0_A</code>, Bob can seize entire channel.)</p> <p><strong>Step 7</strong>: State <code class="language-plaintext highlighter-rouge">S1</code> becomes the current state of the channel. (All previous states become obsolete, and neither party has any incentive to broadcast them, since doing so could lead to a loss of funds.)</p> <p>The critical invariant is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>new state becomes safe before old state is revoked
</code></pre></div></div> <p>This is what prevents a party from being trapped in a window where the old state is invalid but the new state is not yet spendable.</p> <h4 id="why-the-protocol-is-safe">Why the protocol is safe</h4> <p>In this case, Alice is paying Bob and wants to move from S0 to S1.</p> <ul> <li>Bob first sends Alice the signature needed for <strong>Alice’s</strong> S1 commitment transaction</li> <li>only after Alice already has a safe S1 close does she reveal the revocation secret for S0</li> <li>then Alice sends Bob the signature for <strong>Bob’s</strong> S1 commitment transaction</li> </ul> <p>So if someone tries to broadcast S0 <em>before</em> revocation, S0 was still valid. No theft occurs. If someone broadcasts S0 <em>after</em> revocation, the counterparty can punish. There is no unsafe middle ground.</p> <h3 id="33-force-close-vs-cooperative-close">3.3 Force Close vs Cooperative Close</h3> <p>A Lightning channel can end in two main ways.</p> <h4 id="cooperative-close">Cooperative close</h4> <p>In a cooperative close, both parties agree on the final balance and construct a simple closing transaction spending the funding output directly to their chosen destination addresses. This is the preferred path because it avoids delay scripts and reduces fees.</p> <p>In our running example, after reaching <code class="language-plaintext highlighter-rouge">S3</code>:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Alice = 1.2 BTC
Bob   = 0.8 BTC
</code></pre></div></div> <p>In a cooperative close, both Alice and Bob work together to construct a final closing transaction:</p> <blockquote> <p><strong>Input</strong>:</p> <ul> <li>funding_output (2 BTC)</li> </ul> <p><strong>Outputs</strong>:</p> <ul> <li>to Alice’s P2WPKH: 1.2 BTC</li> <li>to Bob’s P2WPKH: 0.8 BTC</li> </ul> </blockquote> <p>After it is signed with all valid signatures, and then broadcast to the Bitcoin network.</p> <h4 id="force-close">Force close</h4> <p>If one party disappears or cooperation fails, the other can unilaterally broadcast its latest commitment transaction. In that case:</p> <ul> <li>the counterparty’s <code class="language-plaintext highlighter-rouge">to_remote</code> output is usually immediately spendable</li> <li>the broadcaster’s <code class="language-plaintext highlighter-rouge">to_local</code> output is delayed</li> </ul> <p>This asymmetry is intentional. It ensures that if the broadcaster used an outdated state, the honest party has time to punish.</p> <h3 id="34-what-does-bitcoin-actually-see">3.4 What does Bitcoin actually see?</h3> <p>In our running channel example, the Bitcoin chain normally sees only:</p> <ol> <li>the funding transaction creating the multisig P2WSH channel output</li> <li>the closing transaction redistributing the final balances</li> </ol> <p>All balance updates, revocation exchanges, and routed forwarding logic remain off-chain unless there is a dispute or force close. This means that Bitcoin does not record all Lightning payments. It records only the opening and final settlement, unless a problem forces an intermediate state to touch the chain.</p> <h3 id="35-why-watchtowers-are-needed">3.5 Why Watchtowers are needed</h3> <p>Lightning security assumes someone is watching the blockchain for revoked-state broadcasts. But users are not always online. Mobile wallets sleep, laptops disconnect, and noncustodial users may not continuously monitor the chain. Watchtowers are services that monitor on behalf of users. The basic idea is:</p> <ol> <li>the user prepares encrypted penalty data for each revocable state</li> <li>the watchtower stores this data without learning the channel details</li> <li>the watchtower scans the chain for matching breach hints</li> <li>if a revoked commitment transaction appears, the tower broadcasts the penalty transaction</li> </ol> <p>In effect, the watchtower acts as an outsourced alarm bell and responder. It does not need custody of the funds, and well-designed watchtower schemes preserve privacy by storing only encrypted blobs and small identifiers.</p> <p>In a cooperative close, the tower never needs to act. But its mere existence makes offline use safer.</p> <p>See the <a href="https://incml.github.io/BTC-Explorer/btc-lightning-channel.html">Lightning Channel Explorer</a> for an interactive, step-by-step walkthrough of this example, with all the technical details explained in the context of the Lightning Network.</p> <hr/> <h2 id="4-revocation-how-cheating-is-punished">4 Revocation: How Cheating Is Punished</h2> <p>Lightning’s core security idea is simple:</p> <blockquote> <p>publishing an old state should be catastrophic for the cheater.</p> </blockquote> <p>If Alice broadcasts an outdated commitment transaction after it has been revoked, Bob should be able to take the entire channel balance, not merely restore the fair split. This severe penalty makes cheating economically irrational.</p> <p>The mechanism works because each old state has its own revocation key. When state <code class="language-plaintext highlighter-rouge">S1</code> is superseded by <code class="language-plaintext highlighter-rouge">S2</code>, the necessary secret information is released so that the counterparty can derive the revocation private key corresponding to <code class="language-plaintext highlighter-rouge">S1</code>.</p> <p>Thus:</p> <ul> <li>before revocation, an old state may still be valid</li> <li>after revocation, that same old state becomes a trap</li> </ul> <p>This is why Lightning can safely keep many obsolete commitment transactions in history without trusting either party to “forget” them.</p> <p>If <code class="language-plaintext highlighter-rouge">S1</code> is later broadcast on-chain by Alice, Bob does the following:</p> <ol> <li>Detect the old commitment transaction <code class="language-plaintext highlighter-rouge">S1</code> on-chain.</li> <li>Locate the spendable output that contains the revocation/delay script.</li> <li>Construct a penalty transaction spending that output.</li> <li>In the witness or script input, they choose the revocation OP_IF branch of the script.</li> <li>They provide a valid signature using the revocation secret.</li> <li>Because the revocation branch has no delay, the output can be spent immediately.</li> </ol> <h4 id="per-commitment-secrets-and-revocation-key-derivation">Per-Commitment Secrets and Revocation Key Derivation</h4> <p>In Lightning, each commitment state $i$ has a fresh per-commitment secret $s_i$ and corresponding public point $P_i = s_i G$. Each party also has a long-lived revocation base secret b with public basepoint $B = b G$. For each state, these two public points are combined to form a revocation public key</p> \[R_i = B \cdot H(P_i \| B) + P_i \cdot H(B \| P_i)\] <p>which can be placed into the commitment script immediately, because it depends only on public data. The matching private key is</p> \[r_i = b \cdot H(P_i \| B) + s_i \cdot H(B \| P_i)\] <p>but this cannot be computed at first, since it requires both the long-lived revocation base secret and the state-specific per-commitment secret. The missing per-commitment secret is disclosed only when that state is later revoked. As a result, the revocation path is impossible to use too early, yet becomes fully spendable exactly when the state becomes obsolete. This is what allows Lightning to pre-commit a penalty key on-chain while activating it only after revocation.</p> <hr/> <h2 id="5-shachain-storing-many-revocation-secrets-efficiently">5. Shachain: Storing Many Revocation Secrets Efficiently</h2> <p>A long-lived channel may update thousands or millions of times. Naïvely storing one fresh revocation secret per state would be wasteful. Lightning solves this with <strong>shachain</strong>, a compact hash-based structure that allows a large sequence of revocation secrets to be represented with only O(log N) storage. In practice, for a 48-bit state space, a node needs to store at most about 48 secrets rather than millions. Conceptually, secrets are organized in a binary derivation structure. As newer secrets are received, older ones can often be derived and no longer need to be stored individually.</p> <p>Real shachain is more subtle than a simple linear hash chain, but the key intuition is the same: the protocol exploits structure in the state numbering so that memory use stays tiny even for huge channels. Shachain matters because Lightning’s revocation system only works well if implementations can efficiently manage historical state.</p> <hr/> <h2 id="6-htlcs-extending-channels-into-a-network">6. HTLCs: Extending Channels into a Network</h2> <p>Channels alone only solve direct bilateral payments. To build a network, Lightning uses <strong>Hashed Timelock Contracts</strong> (HTLCs).</p> <p>An HTLC combines two ideas:</p> <ul> <li>a <strong>hashlock</strong>: funds can be claimed only by revealing a secret preimage</li> <li>a <strong>timelock</strong>: if the secret is not revealed in time, the sender can refund</li> </ul> <p>A conceptual HTLC says:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>If SHA256(secret) = H, pay the receiver.
Otherwise, after timeout, refund the sender.
</code></pre></div></div> <p>This allows value to move across several channels without requiring trust in intermediaries.</p> <h4 id="multi-hop-payment-example">Multi-Hop Payment Example</h4> <p>Suppose Alice wants to pay Bob 0.1 BTC through Carol and Dave:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Alice → Carol → Dave → Bob
</code></pre></div></div> <p>First, Bob chooses a random secret value <code class="language-plaintext highlighter-rouge">s</code> and computes its hash</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>H = SHA256(s)
</code></pre></div></div> <p>Bob sends only the hash <code class="language-plaintext highlighter-rouge">H</code> back to Alice, while keeping the actual secret s private.</p> <p>Alice then offers Carol an HTLC that says “Carol may take 0.1 BTC if she can provide a value <code class="language-plaintext highlighter-rouge">s</code> such that <code class="language-plaintext highlighter-rouge">SHA256(s)=H</code> before a certain deadline. Otherwise, after the deadline, Alice gets the money back.” So this HTLC has two spending paths:</p> <ul> <li><strong>Success path</strong>: Carol presents the correct preimage <code class="language-plaintext highlighter-rouge">s</code> before timeout</li> <li><strong>Timeout path</strong>: after expiry, Alice reclaims the funds</li> </ul> <p>This HTLC is recorded off-chain by Alice and Carol to update their channel commitment state. Before the HTLC, maybe the balance is:</p> <ul> <li>Alice: 1.0 BTC</li> <li>Carol: 1.0 BTC</li> </ul> <p>After inserting the HTLC for 0.1 BTC, the state is updated as:</p> <ol> <li>Alice: 0.9 BTC immediately spendable</li> <li>Carol: 1.0 BTC immediately spendable</li> <li>plus one pending HTLC output of 0.1 BTC, claimable by Carol with preimage <code class="language-plaintext highlighter-rouge">s</code>, otherwise refundable to Alice after timeout</li> </ol> <p>A simplified pseudocode for the above third HTLC branch is</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>IF
    Carol provides secret s
    AND SHA256(s) == H
    AND Carol's signature is valid
THEN
    Carol can spend
ELSE
    after timeout 
    Alice with her signature can spend
ENDIF
</code></pre></div></div> <p>Carol does the same with Dave, and Dave does the same with Bob. Thus, each hop sets up a conditional payment tied to the same hash <code class="language-plaintext highlighter-rouge">H</code>, but with different timeout values.</p> <p>At this point, Bob is the only party who knows the secret <code class="language-plaintext highlighter-rouge">s</code>. Therefore, only Bob can satisfy the final HTLC from Dave. When Bob claims that HTLC, he must reveal s. Once Dave sees s, he can use the same secret to claim his incoming HTLC from Carol. Carol then learns s and uses it to claim from Alice.</p> <p>So the mechanism has a very elegant structure:</p> <ul> <li>the conditional payment commitments are created from Alice forward to Bob,</li> <li>but the secret <code class="language-plaintext highlighter-rouge">s</code> is revealed and propagates backward from Bob to Alice.</li> </ul> <p>This is what makes the payment effectively atomic across the whole route. Bob cannot receive the money without revealing <code class="language-plaintext highlighter-rouge">s</code>, and once <code class="language-plaintext highlighter-rouge">s</code> is revealed, each upstream hop can use it to collect its own incoming payment. If Bob never reveals the secret, then nobody downstream can complete the claim, and each HTLC eventually expires and refunds safely.</p> <p>The timeouts are deliberately staggered. For example, Alice’s HTLC to Carol expires later than Carol’s HTLC to Dave, which expires later than Dave’s HTLC to Bob. This gives each intermediary a safety window: after learning <code class="language-plaintext highlighter-rouge">s</code> from the downstream hop, it still has enough time to redeem its upstream HTLC before that one expires. Without staggered timeouts, an intermediary could be exposed to loss by paying downstream but not having enough time to collect upstream.</p> <p>In summary, HTLC routing lets strangers forward payments without trusting one another: either the secret is revealed and everyone along the route can claim in order, or the secret never appears and the contracts simply time out and refund.</p> <hr/> <h2 id="7-practical-advantages-of-lightning">7. Practical Advantages of Lightning</h2> <p>Lightning’s security is not based on preventing cheating in an absolute sense. Instead, it is based on making cheating detectable and catastrophically expensive.</p> <p>The security stack includes:</p> <ul> <li>the funded 2-of-2 output anchoring the channel on Bitcoin</li> <li>commitment transactions giving each side a unilateral exit</li> <li>relative timelocks delaying the broadcaster’s own funds</li> <li>revocation keys making obsolete states punishable</li> <li>HTLC timeouts preserving atomicity in routed payments</li> <li>watchtowers assisting offline monitoring</li> </ul> <p>The key economic rule is:</p> <blockquote> <p>honest behavior yields your fair balance; dishonest behavior risks losing everything.</p> </blockquote> <p>This is not merely a social norm or implementation trick. It is encoded into Bitcoin spend conditions.</p> <p>Lightning provides several major benefits over repeated on-chain payments:</p> <ul> <li><strong>speed:</strong> no need to wait for block confirmations for every payment</li> <li><strong>cost:</strong> fees are typically tiny compared with on-chain fees</li> <li><strong>scalability:</strong> many updates compress into two on-chain transactions</li> <li><strong>privacy:</strong> intermediate balance changes are not globally published</li> <li><strong>global reach:</strong> users can pay peers with whom they do not share direct channels</li> </ul> <p>These advantages make Lightning particularly attractive for micropayments, point-of-sale transactions, machine-to-machine transfers, and other interactive payment flows.</p> <hr/> <h2 id="8-conclusion">8. Conclusion</h2> <p>The Lightning Network is one of the most sophisticated constructions built on Bitcoin. It takes a minimal scripting system and, through careful protocol engineering, turns it into a high-speed payment network.</p> <p>Its core ideas can be summarized as follows:</p> <ol> <li>lock funds in a shared multisig funding output</li> <li>represent balance ownership with asymmetric commitment transactions</li> <li>revoke old states so that cheating becomes punishable</li> <li>use relative timelocks to create a dispute window</li> <li>route across the network with HTLCs</li> <li>rely on Bitcoin for final settlement and dispute resolution</li> </ol> <p>The result is a layered architecture in which Bitcoin remains the secure settlement base while Lightning serves as a fast payment layer.</p>]]></content><author><name>Hui Jiang</name></author><category term="btc,"/><category term="ligtning,"/><category term="revocation,"/><summary type="html"><![CDATA[This post presents a technical but accessible introduction to Lightning, starting from Bitcoin transaction primitives and building upward to channel construction, state updates, watchtowers, revocation, HTLC routing.]]></summary></entry><entry><title type="html">Elliptic Curve Digital Signatures in Bitcoin</title><link href="https://incml.github.io/huijiang/blog/2026/BTC-Signatures/" rel="alternate" type="text/html" title="Elliptic Curve Digital Signatures in Bitcoin"/><published>2026-02-21T00:00:00+00:00</published><updated>2026-02-21T00:00:00+00:00</updated><id>https://incml.github.io/huijiang/blog/2026/BTC-Signatures</id><content type="html" xml:base="https://incml.github.io/huijiang/blog/2026/BTC-Signatures/"><![CDATA[<blockquote> <p><strong>Abstract</strong></p> <p>We explain the cryptographic use of elliptic curves by first building the underlying <em>finite abelian group</em> structure, and then deriving <strong>ECDSA</strong> and <strong>Schnorr</strong> signatures along with the multi-signature (multisig) scheme, which are currently used in the Bitcoin network. <em>See <a href="https://incml.github.io/BTC-Explorer/btc-signature-lab/index.html">Bitcoin Signature Lab</a> for an interactive demo to demenstrate all digital signature methods explained in this post.</em></p> </blockquote> <h1 id="1-elliptic-curves-as-finite-groups">1. Elliptic Curves as Finite Groups</h1> <p>This section first explains the foundation model you need for ECDSA/Schnorr: <strong>elliptic curves</strong> are a way to construct a finite group in which a discrete logorithm problem is hard.</p> <h2 id="11-curves-over-different-domains">1.1 Curves over different domains</h2> <p>An elliptic curve is typically presented (short Weierstrass form) as</p> \[E: \quad y^2 = x^3 + ax + b.\] <p>What “points on the curve” means depends on the underlying set for \((x,y)\):</p> <ul> <li>Over $\mathbb{R}$: infinitely many points; you can draw the familiar smooth curve.</li> <li>Over a finite field $\mathbb{F}_p$ (prime $p$): <strong>only finitely many points</strong>, hence a <strong>finite group</strong>.</li> </ul> <p>In modern ECC (including Bitcoin), we almost always use a finite field $\mathbb{F}_p$ with large prime $p$.</p> <h2 id="12-why-mathbbf_p-automatically-gives-finitely-many-points">1.2 Why $\mathbb{F}_p$ automatically gives finitely many points</h2> <p>In $\mathbb{F}_p$,</p> \[\mathbb{F}_p = \{0,1,2,\dots,p-1\},\] <p>so there are only $p$ possible $x$-values and $p$ possible $y$-values. Therefore, the set of candidate pairs $(x,y)$ is at most $p^2$, and the curve’s solutions are a subset. Hence the size of $E(\mathbb{F}_p)$, called <strong>order</strong>, is finite.</p> <p>More precisely, for each fixed $x$, the equation</p> \[y^2 \equiv r \pmod p,\quad r \equiv x^3 + ax + b \pmod p\] <p>has either $0$, $1$, or $2$ solutions for $y$. This already suggests the total number of points is on the order of $p$.</p> <p>We require the curve to be <strong>non-singular</strong>, which is equivalent to a nonzero discriminant:</p> \[\Delta = -16(4a^3 + 27b^2) \not\equiv 0 \pmod p.\] <p>This condition prevents cusps/self-intersections and ensures the group law behaves well (notably, associativity).</p> <h2 id="13-the-strange-addition-law-geometry--algebra">1.3 The “strange” addition law: geometry → algebra</h2> <p>Over $\mathbb{R}$, we define point addition via the line-through-two-points picture:</p> <ul> <li>Given $P$ and $Q$, draw the line $L$ through them.</li> <li>It intersects the cubic at a third point $R$ (counting multiplicity).</li> <li>Define $P + Q$ to be the reflection of $R$ across the $x$-axis.</li> </ul> <p>In a finite field $\mathbb{F}_p$, you cannot literally “draw” a line—yet a similar rule is used instead according to <strong>pure algebra</strong>. For example, write the line as</p> \[y = mx + c,\] <p>substitute it into the curve, and you obtain a cubic equation in $x$:</p> \[(mx+c)^2 = x^3 + ax + b \quad \Longrightarrow \quad x^3 - m^2x^2 + (a-2mc)x + (b-c^2)=0.\] <p>If $L$ passes through $P$ and $Q$, then $x_P$ and $x_Q$ are roots of this cubic, so there is a third root $x_R$ (in the algebraic closure). Moreover, because the coefficients and $x_P,x_Q$ lie in $\mathbb{F}_p$, Vieta’s relations imply</p> \[x_P + x_Q + x_R = m^2 \in \mathbb{F}_p \quad \Longrightarrow \quad x_R \in \mathbb{F}_p.\] <p>So the “third intersection point” is not a topological fact—it is a <strong>polynomial fact</strong>. Here, this third intersection point is defined as the result of adding $P$ to $Q$ in $\mathbb{F}_p$, i.e. $P+Q$.</p> <h2 id="14-why-this-forms-a-group">1.4 Why this forms a group</h2> <p>With the above definitions, one can show $E(\mathbb{F}_p)$ is an <strong>abelian group</strong>:</p> <ul> <li><strong>Closure:</strong> algebraic third-intersection argument.</li> <li><strong>Identity:</strong> $O$, which is the <strong>point at infinity</strong>. As we will see later, we have the fact that $ P + O = O + P = P$, which makes $O$ the identity.</li> <li><strong>Inverse:</strong> the inverse of $P=(x,y)$ is defined as $ -P = (x,-y).$ As we will see later, we have $ P +(-P) = O$.</li> <li><strong>Commutativity:</strong> $P+Q=Q+P$ (same line).</li> </ul> <p>For cryptography, we rely on this group structure being correct and efficiently computable.</p> <h2 id="15-how-to-compute-pq-explicitly-in-mathbbf_p">1.5 How to compute $P+Q$ explicitly in $\mathbb{F}_p$</h2> <p>Let the curve be in short Weierstrass form over a prime field $\mathbb{F}_p$:</p> \[E: \quad y^2 \equiv x^3 + ax + b \pmod p,\] <p>and let</p> \[P=(x_1,y_1),\quad Q=(x_2,y_2)\in E(\mathbb{F}_p).\] <p>All arithmetic below is performed <strong>modulo $p$</strong> (for coordinates).</p> <h3 id="case-a-add-to-identity">Case A: Add to Identity</h3> <p>In this case, we have $P = O$ or $Q = O$. Therefore, we define</p> \[P + O = P,\qquad O + Q = Q.\] <h3 id="case-b-vertical-line">Case B: Vertical line</h3> <p>In this case $x_1 = x_2$ and $y_1 \equiv -y_2 \pmod p$. The line through $P$ and $Q$ is “vertical”, so the sum is the identity:</p> \[P + Q = O.\] <p>This is exactly the group-theoretic inverse rule, since $Q=-P$ in this case. Therefore, we have $ P - P = P +(-P) = O$.</p> <h3 id="case-c-general-addition">Case C: General Addition</h3> <p>In this case, $P \neq Q$. We compute the slope</p> \[m \equiv \frac{y_2 - y_1}{x_2 - x_1} \pmod p \equiv (y_2 - y_1)\cdot (x_2 - x_1)^{-1} \pmod p.\] <p>Then compute the third-intersection algebraically (the point where the line meets the curve “the third time”):</p> \[x_3 \equiv m^2 - x_1 - x_2 \pmod p,\] \[y_3 \equiv m(x_1 - x_3) - y_1 \pmod p.\] <p>Define</p> \[P+Q = (x_3,y_3).\] <blockquote> <p><strong>Note.</strong> The “reflect across the $x$-axis” step is already built into the formula for $y_3$ above.</p> </blockquote> <h3 id="case-d-doubling">Case D: Doubling</h3> <p>In this case, we have $P = Q$, i.e., $P + Q = 2P$.</p> <p>If $y_1 \equiv 0 \pmod p$, then the tangent is vertical and</p> \[2P = O.\] <p>Otherwise compute the tangent slope:</p> \[m \equiv \frac{3x_1^2 + a}{2y_1} \pmod p \equiv (3x_1^2 + a)\cdot (2y_1)^{-1} \pmod p,\] <p>and then reuse the same coordinate formulas:</p> \[x_3 \equiv m^2 - 2x_1 \pmod p,\] \[y_3 \equiv m(x_1 - x_3) - y_1 \pmod p.\] <p>So</p> \[2P = (x_3,y_3).\] <h3 id="division-in-mathbbf_p-computing-the-inverse-cdot-1">Division in $\mathbb{F}_p$: computing the inverse $(\cdot)^{-1}$</h3> <p>The term $(x_2-x_1)^{-1}$ (or $(2y_1)^{-1}$) means the <strong>multiplicative inverse mod $p$</strong>. Since $p$ is prime, every nonzero value has an inverse. Two standard ways:</p> <ul> <li><strong>Extended Euclid:</strong> find $u,v$ such that $(x_2-x_1)u + pv = 1$, then $u \equiv (x_2-x_1)^{-1} \pmod p$.</li> <li> <p><strong>Fermat:</strong> for nonzero $t$,</p> \[t^{-1} \equiv t^{p-2} \pmod p.\] </li> </ul> <p>In real-world ECC implementations, inversions are relatively expensive; many libraries use projective coordinates (e.g., Jacobian coordinates) to replace most inversions with multiplications, doing a single inversion at the end.</p> <h2 id="16-from-finite-group-to-cryptography-scalar-multiplication">1.6 From finite group to cryptography: scalar multiplication</h2> <p>Given a point $G$, define <strong>scalar multiplication</strong>:</p> \[kG = \underbrace{G + G + \cdots + G}_{k\text{ times}}.\] <p>Efficient algorithms (double-and-add, windowed methods) compute $kG$ in $O(\log k)$ group operations.</p> <p>The core hardness assumption is that, given $G$ and $Q=dG$, computing $d$ is infeasible, which is called Elliptic Curve Discrete Logarithm Problem (<strong>ECDLP</strong>).</p> <h2 id="17-bitcoins-concrete-curve-secp256k1">1.7 Bitcoin’s concrete curve: secp256k1</h2> <p>Bitcoin uses the curve secp256k1 over $\mathbb{F}_p$ with</p> \[p = 2^{256} - 2^{32} - 977, \qquad E: y^2 \equiv x^3 + 7 \pmod p.\] <p>A base point $G$ generates a subgroup of large prime order $n$. Private keys are scalars $d\in[1,n-1]$, public keys are $Q=dG$.</p> <p>In the Bitcoin network, we choose $G = (x_G, y_G)$ with</p> <p><code class="language-plaintext highlighter-rouge">x_G=0x79BE667EF9DCBBAC55A06295CE870B07029BFCDB2DCE28D959F2815B16F81798</code> <code class="language-plaintext highlighter-rouge">y_G=0x483ADA7726A3C4655DA4FBFC0E1108A8FD17B448A68554199C47D08FFB10D4B8</code></p> <p>and the order $n$, i.e., the smallest positive integer such as $n G = O$ is</p> <p><code class="language-plaintext highlighter-rouge">n = FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFEBAAEDCE6AF48A03BBFD25E8CD0364141.</code></p> <blockquote> <p><strong>Takeaway:</strong> Elliptic curves on $\mathbb{F}_p$ give us a <em>finite</em> group with a hard discrete logarithm problem and fast group operations. Signatures (ECDSA, Schnorr) are built on top of this exact structure.</p> </blockquote> <hr/> <h1 id="2-elliptic-curve-digital-signatures">2. Elliptic Curve Digital Signatures</h1> <p>This section provides a rigorous and structured treatment of:</p> <ul> <li><strong>ECDSA (Elliptic Curve Digital Signature Algorithm)</strong></li> <li><strong>Schnorr Signatures (BIP340 / Taproot in Bitcoin)</strong></li> </ul> <p>Both rely on the hardness of the <strong>Elliptic Curve Discrete Logarithm Problem (ECDLP)</strong>:</p> \[\text{Given }G \text{ and } Q = dG, \text{recover} \; d\] <p>We assume a cyclic elliptic curve group of prime order $ n $ with generator $ G $.</p> <p>Private key: \(d \in \{ 1, 2, \cdots, n-1\}\)</p> <p>Public key: \(Q = dG\)</p> <p>All scalar arithmetic is performed modulo $ n $.</p> <p>Due to the computational hardness of the Elliptic Curve Discrete Logarithm Problem (<strong>ECDLP</strong>), it is infeasible to recover the private key $d$ from the corresponding public key $Q$. In a digital signature scheme, we assume that the holder of the private key—referred to as the signer—intends to authenticate a piece of information, called a <strong>message</strong>. The fundamental idea behind digital signatures is to design a secure signing algorithm in which the signer uses the private key to process the message and produce a value known as a <strong>signature</strong>, which is used to acknowledge the message. The signing procedure must be constructed so that no party can generate a valid signature without knowledge of the private key. The second phase is verification. In this phase, any party can execute a verification algorithm that uses only the public key, together with the <strong>message</strong> and the <strong>signature</strong>, to undo the processing in the signing stage to be able to check a specific deterministic condition. If the verification succeeds, it provides strong assurance that the signature was produced by the legitimate signer, since only the signer possesses the private key required to generate a valid signature.</p> <hr/> <h2 id="21-ecdsa">2.1 ECDSA</h2> <h3 id="211-signing-algorithm-message--private-key----signature">2.1.1 Signing Algorithm: message + private key → signature</h3> <p>Given a message $ m $:</p> <h5 id="step-1-hash-the-message">Step 1: Hash the message</h5> \[e = H(m)\] <h5 id="step-2-choose-nonce">Step 2: Choose nonce</h5> \[\text{choose randomly:} \;\;\; \;\; k \in \{ 1, 2, \cdots, n-1\}\] <h5 id="step-3-compute-an-ec-point">Step 3: Compute an EC point</h5> \[R = kG = (x_R, y_R)\] <p>where choose \(r = x_R \; \pmod{n}\)</p> <h5 id="step-4-compute-signature">Step 4: Compute signature</h5> \[s = k^{-1}(e + rd) \; \text{mod} \; n\] <p><strong>Signature</strong> is produced as $ (r, s). $</p> <h3 id="212-verification-message--signature--public-key----acceptreject">2.1.2 Verification: message + signature + public key → accept/reject</h3> <p>Given the signature $ (r,s) $ and the message $m$:</p> <h5 id="step-1-hash-the-message-m">Step 1: Hash the message $m$</h5> \[e = H(m)\] <h5 id="step-2-process-the-message-hash-e-with-signature-rs">Step 2: process the message hash $e$ with signature $(r,s)$</h5> \[w = s^{-1} \; \text{mod} \; n\] \[u_1 = ew \; \text{mod} \; n\] \[u_2 = rw \; \text{mod} \; n\] <h5 id="step-3-produce-the-result-using-the-public-key-q">Step 3: produce the result using the public key $Q$</h5> \[P = u_1 G + u_2 Q\] <h5 id="step-4-verification">Step 4: verification</h5> <p>Accept if:</p> \[x_P \equiv r \; \text{mod} \; n\] <h3 id="213-why-it-works">2.1.3 Why It Works</h3> <p>From the signing formula: \(s = k^{-1}(e + rd)\)</p> <p>Rearrange: \(k = s^{-1}(e + rd)\)</p> <p>Verification undoes the signing process to reconstruct the same $R$:</p> \[P = e s^{-1} G + r s^{-1} Q = s^{-1}(e + rd)G = kG = R\] <h3 id="214-ecdsa-diagram">2.1.4 ECDSA Diagram</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Signer: m + d → (r,s)

   m → e = H(m)
   k → R = kG
   r = x_R
   s = k⁻¹(e + r d)
   

Verifier: m + (r,s) + Q → valid/not

   m → e = H(m)
   w = s⁻¹
   P = e w G + r w Q
   Check x_P = r
</code></pre></div></div> <blockquote> <p><strong>Note.</strong> In ECDSA (including Bitcoin), if you have a valid signature $(r, s)$, then $(r, n - s)$ is also valid, where n is the secp256k1 curve order. So without a rule, one signature has two valid forms (high-S and low-S). Low-S normalization means: • If $s &gt; n/2$, replace it with $s = n - s$. • Otherwise keep $s$ as is. This forces a unique canonical form (the “lower half” value of $s$), which helps prevent signature malleability in Bitcoin policy/consensus contexts.</p> </blockquote> <hr/> <h2 id="22-schnorr-signature-bip340">2.2 Schnorr Signature (BIP340)</h2> <h3 id="221-signing">2.2.1 Signing</h3> <p>Given message $ m $:</p> <ul> <li>Choose a random $ k \in { 1,2, \cdots, n-1 }$:</li> </ul> \[R = kG\] <ul> <li>Compute challenge:</li> </ul> \[e = H(R \| Q \| m)\] <ul> <li>Compute response:</li> </ul> \[s = k + e \,d \; \pmod{n}\] <p>Signature is generated as \((R, s).\)</p> <h3 id="222-verification">2.2.2 Verification</h3> <ul> <li>Compute challenge in the same way as above:</li> </ul> \[e = H(R \| Q \| m)\] <ul> <li>Compute:</li> </ul> \[P = s G - e Q\] <ul> <li>Accept if:</li> </ul> \[x_P \equiv x_R \; \text{mod} \; n\] <h3 id="223-why-it-works">2.2.3 Why It Works</h3> <p>From the signing formula:</p> \[s = k + ed\] <p>Multiply by $ G $:</p> \[sG = kG + edG = R + eQ\] \[\implies P = s G - e Q = R\] <h2 id="224-why-do-we-compute-the-challenge-e-in-schnorr">2.2.4 Why Do We Compute the Challenge e in Schnorr?</h2> <p>In the Schnorr signing algorithm, after computing the nonce point</p> \[R = kG,\] <p>we compute the <strong>challenge</strong> using a cryptographic hash function \(H(\cdot)\) as follows:</p> \[e = H(R \,\|\, Q \,\|\, m),\] <p>which binds everything together:</p> <ul> <li>\(R\) = nonce commitment</li> <li>\(Q\) = public key</li> <li>\(m\) = message</li> </ul> <p>This step is essential for <strong>security and correctness</strong>. Below is why.</p> <p>First of all, if we did not hash the message into the signature equation, the signature could be reused for different messages. Because \(e\) depends on \(m\), changing the message changes \(e\), and therefore changes the final signature \(s\). This guarantees that signature validity is message-dependent.</p> <p>Secondly, if the nonce commitment \(R\) is not included in the hash that defines the challenge \(e\), the Schnorr signature scheme becomes forgeable. Specifically, suppose the challenge is computed as \(e = H( Q \,\|\, m)\), which does not depend on \(R\). A forger can then choose an arbitrary scalar \(s'\) and construct a forged signature \((R', s')\) by setting \(R' = s' G - e Q\). This pair will pass verification, because the verifier checks \(s' G = R' + e Q\), which is always true from the way to choose \(R'\). Thus, the forged signature satisfies the verification equation without requiring knowledge of the private key. In contrast, when the challenge is defined as \(e = H( R \,\|\, Q \,\|\, m)\), the scheme is secure against this attack. In this case, the challenge depends on \(R\), so a forger cannot freely choose \(s'\) and then compute a corresponding \(R'\). Any attempt to construct \(R'\) requires knowing \(e\) but \(e\) itself depends on \(R'\), creating a circular dependency. This circularity prevents the attacker from generating a valid signature without the private key.</p> <p>Finally, if the challenge \(e\) is not bound to the public key \(Q\), the scheme becomes vulnerable to a key-substitution (rogue-key) attack. Suppose the challenge is computed as \(e = H( R \,\|\, m)\), so it does not depend on the public key. Given a valid signature \((R,s)\) on message \(m\) under public key \(Q\), an attacker can construct a different public key</p> \[Q' = \frac{s G - R }{e} = e^{-1} (s G - R )\] <p>Now consider verification under this new public key \(Q'\). The verifier checks whether \(P = R\) holds. Substituting the definition of \(Q'\), the verification equation holds:</p> \[P = sG - e Q' = sG - e \cdot e^{-1} (s G - R ) = R\] <p>Consequently, the same signature \((R,s)\) is valid for the same message \(m\) under both the original public key \(Q\) and the attacker-constructed public key \(Q'\). In other words, anyone can take a genuine signature and fabricate a public key that makes the signature verify. This breaks the fundamental security property of digital signatures: the signature no longer proves that the holder of the original public key \(Q\) produced it. Binding the challenge to the public key, i.e., \(e = H( R \,\|\, Q \,\|\, m)\) prevents this attack, because the challenge would change if the public key were altered.</p> <h4 id="it-preserves-linearity-crucial-for-musig">It Preserves Linearity (Crucial for MuSig)</h4> <p>The form</p> \[s = k + ed\] <p>is linear in the secret key \(d\).</p> <p>Since \(e\) is a scalar derived from a hash, it preserves linear structure.<br/> This linearity enables:</p> <ul> <li>Key aggregation</li> <li>Signature aggregation</li> <li>MuSig multi-signatures</li> </ul> <blockquote> <p><strong>Takeaway:</strong> The computation of \(e = H(R \,\|\, P \,\|\, m)\) is what transforms Schnorr from a simple linear equation into a secure digital signature scheme.</p> </blockquote> <hr/> <h3 id="224-schnorr-diagram">2.2.4 Schnorr Diagram</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Signer: m + d → (R,s)

   k → R = kG
   e = H(R || Q || m)
   s = k + e d


Verifier: m + (R,s) + Q → valid/not

   Compute e = H(R || Q || m)
   compute P = sG - e Q
   Check P = R
</code></pre></div></div> <hr/> <h2 id="23-ecdsa-vs-schnorr">2.3 ECDSA vs Schnorr</h2> <table> <thead> <tr> <th>Feature</th> <th>ECDSA</th> <th>Schnorr</th> </tr> </thead> <tbody> <tr> <td>Formula</td> <td>$ s = k^{-1}(e + rd) $</td> <td>$ s = k + ed $</td> </tr> <tr> <td>Linear</td> <td>No</td> <td>Yes</td> </tr> <tr> <td>Multi-signature</td> <td>Difficult</td> <td>Natural</td> </tr> <tr> <td>Aggregation</td> <td>No</td> <td>Yes</td> </tr> <tr> <td>Used in Bitcoin</td> <td>Legacy</td> <td>Taproot</td> </tr> </tbody> </table> <hr/> <h1 id="3-multi-signatures-musig">3. Multi-Signatures (MuSig)</h1> <p>A multi-signature (MuSig) scheme allows multiple parties to jointly authorize a message by requiring several independent signatures, typically under a policy such as n-of-n (e.g., all n signers must approve). Each participant signs the same message with their own private key, and the combined signatures are verified against the corresponding public keys. MuSig (a Schnorr-based multisignature scheme) improves upon traditional multisig by aggregating multiple public keys into a single combined public key and producing one compact signature that is indistinguishable from a standard single-party Schnorr signature. The signers interact to generate nonces and partial signatures, which are then combined into one final signature. Verification requires only the aggregated public key, enhancing privacy, efficiency, and scalability compared to classical multisig constructions. It is simple to implement multisig using Schnorr Because Schnorr is linear.</p> <p>Let two signers:</p> \[Q_1 = d_1 G, \quad Q_2 = d_2 G\] <p>Aggregate public key:</p> \[Q = Q_1 + Q_2\] <p>Aggregate nonce:</p> \[R = R_1 + R_2\] <p>Aggregate signature:</p> \[s = s_1 + s_2\] <p>Verification can be done as usual:</p> \[P = sG - e Q \overset{?}{=} R\] <p>This property enables a simple 2-of-2 <strong>MuSig</strong> scheme, used in Bitcoin for n-of-n multi-party signing.</p> <hr/> <h1 id="4-concluding-remarks">4. Concluding Remarks</h1> <p>ECDSA remains widely deployed, but Schnorr offers:</p> <ul> <li>Simpler algebra</li> <li>Clean linear structure</li> <li>Aggregation capability</li> <li>Better provable security properties</li> </ul> <p>Schnorr is considered the more modern and elegant construction.</p> <hr/>]]></content><author><name>Hui Jiang</name></author><category term="btc,"/><category term="elliptic"/><category term="curve,"/><category term="digital"/><category term="signature,"/><summary type="html"><![CDATA[In this post, we explain the cryptographic use of elliptic curves by first building the underlying finite abelian group structure, and then deriving ECDSA and Schnorr signatures, as well as the multi-signature (MuSig) scheme, which are currently used in the Bitcoin network.]]></summary></entry><entry><title type="html">Natural Languages as ε-Ambiguity Languages</title><link href="https://incml.github.io/huijiang/blog/2026/epsilon-language/" rel="alternate" type="text/html" title="Natural Languages as ε-Ambiguity Languages"/><published>2026-01-12T00:00:00+00:00</published><updated>2026-01-12T00:00:00+00:00</updated><id>https://incml.github.io/huijiang/blog/2026/epsilon-language</id><content type="html" xml:base="https://incml.github.io/huijiang/blog/2026/epsilon-language/"><![CDATA[<h2 id="the-ε-ambiguity-framework"><strong>The ε-Ambiguity Framework</strong></h2> <p>Natural languages support reliable communication despite variability, noise, and structural underspecification. Unlike programming languages, they allow metaphor, ellipsis, ambiguity, deixis, and context-dependent meaning. Yet humans typically recover the intended meaning with high accuracy.</p> <p>Recent theoretical work <d-cite key="Jiang2023LatentSpaceEmergent"></d-cite> introduces <strong>ε-ambiguity languages</strong> as a formal tool to model this phenomenon. Under this framework, a language is ε-ambiguous if, for any meaningful message <em>x</em>, there exists a dominant intended meaning θ₀ such that</p> \[\Pr(\theta_0 \mid x) \ge 1 - \varepsilon(x), \quad \varepsilon(x) \in [0,1),\] <p>but alternative interpretations occur with small but non-zero probability.</p> <p>In this framework, although many meanings are technically compatible with a linguistic expression, one meaning dominates the posterior probability, and ambiguity occurs only with small probability ε(x). In <d-cite key="Jiang2023LatentSpaceEmergent"></d-cite>, it argues that natural languages empirically exhibit ε-ambiguity, namely they are neither perfectly unambiguous nor fully ambiguous, but instead allow for reliably dominant meanings with bounded ambiguity. Furthermore, <d-cite key="Jiang2023LatentSpaceEmergent"></d-cite> argues that ε-ambiguity provides a coherent explanation for both human semantics and LLM emergent abilities.</p> <h3 id="model-definition">Model Definition</h3> <p>As in <d-cite key="Jiang2023LatentSpaceEmergent"></d-cite>, we assume:</p> <ul> <li>A latent intention space Θ,</li> <li>A surface linguistic expression <em>x</em> generated via a distribution q(x ∣ θ) with θ ∈ Θ.</li> <li>A listener infers meaning via the posterior probability Pr(θ ∣ x).</li> </ul> <p>A language is an <strong>ε-ambiguity language</strong> if for every meaningful expression x:</p> \[\Pr(\theta_0 \mid x) \ge 1 - \varepsilon(x),\] <p>where θ₀ is the intended meaning, and ε(x) quantifies residual ambiguity.</p> <p>This describes a <em>sparse posterior</em> over meanings: a dominant intention and a long but very small tail of alternatives.</p> <h3 id="consequences">Consequences</h3> <p>This model predicts that:</p> <ol> <li>Communication is reliable but not deterministic.</li> <li>Ambiguity decreases <strong>multiplicatively</strong> when multiple cues or messages are provided.</li> <li>Latent-intention inference is feasible even without explicit symbolic structure.</li> </ol> <p>The fundamental principle behind ε-ambiguity languages is that linguistic expressions exhibit <em>partial but not perfect</em> semantic determinacy. Messages tend to convey one meaning with high probability, but never with absolute certainty. Real-world natural languages possess exactly these properties: they support efficient communication despite intrinsic ambiguity, and ambiguity is controlled by contextual, semantic, and pragmatic mechanisms.</p> <p>The ε-ambiguity framework formalizes this intuition within a probabilistic generative model of communication, where meanings (latent intentions θ) are drawn from a space Θ, and surface messages are generated by noisy processes with intention-specific distributions q(x ∣ θ). The framework provides a mathematical explanation for why LLMs can infer hidden meanings from text and why phenomena such as chain-of-thought reasoning reduce uncertainty.</p> <p>The central question of this article is:</p> <blockquote> <p><strong>What empirical and theoretical evidence supports the view that natural languages satisfy the definition of ε-ambiguity languages?</strong></p> </blockquote> <p>We demonstrate that evidence comes from multiple research domains.</p> <hr/> <h2 id="evidence-from-linguistics"><strong>Evidence from Linguistics</strong></h2> <h3 id="lexical-ambiguity-and-polysemy">Lexical Ambiguity and Polysemy</h3> <p>Natural languages contain extensive lexical ambiguity <d-cite key="Lyons1977Semantics"></d-cite> <d-cite key="Cruse1986LexicalSemantics"></d-cite>. Words often possess multiple senses (e.g., <em>bank</em>, <em>seal</em>, <em>interest</em>), yet human speakers reliably infer the dominant meaning from context. Corpus-based studies of word sense disambiguation have shown that sense distributions are often highly skewed, with the most frequent sense accounting for a large majority of occurrences <d-cite key="Kilgarriff1992Polysemy"></d-cite> <d-cite key="Yarowsky1993OneSensePerCollocation"></d-cite> <d-cite key="Kilgarriff2004DominantSense"></d-cite>.</p> <p>This illustrates exactly the condition:</p> \[\Pr(\theta_0 \mid x) \approx 1 - \varepsilon(x), \quad \varepsilon(x) \text{ small but nonzero},\] <p>where θ₀ is the dominant sense.</p> <h3 id="syntactic-ambiguity">Syntactic Ambiguity</h3> <p>Classic syntactic ambiguities (e.g., <em>“I saw the man with the telescope”</em>) allow multiple parses, yet listeners overwhelmingly adopt one interpretation when context is provided. Probabilistic grammars assign steeply skewed probability distributions to parses <d-cite key="CharniakJohnson2005CoarseToFine"></d-cite>, again demonstrating nonzero but concentrated posterior distributions over intentions.</p> <h3 id="pragmatic-inference-and-speech-acts">Pragmatic Inference and Speech Acts</h3> <p>Pragmatics often shifts literal meanings to intended meanings <d-cite key="Grice1975LogicConversation"></d-cite>. For instance:</p> <ul> <li><em>“Can you pass the salt?”</em> is interpreted as a request, not a question about capability.</li> </ul> <p>Research on speech-act recognition <d-cite key="Austin1962HowToDoThings"></d-cite><d-cite key="Searle1969SpeechActs"></d-cite> shows that listeners infer intended acts with high reliability but occasional errors—consistent with ε &gt; 0.</p> <hr/> <h2 id="evidence-from-psycholinguistics"><strong>Evidence from Psycholinguistics</strong></h2> <h3 id="rapid-probabilistic-disambiguation">Rapid Probabilistic Disambiguation</h3> <p>Humans use contextual probabilities to resolve ambiguity almost instantaneously <d-cite key="Tanenhaus1995VisualLinguisticIntegration"></d-cite>. Even in garden-path sentences (e.g., <em>“The horse raced past the barn fell”</em>), misinterpretations occur but are rare relative to successful parsing.</p> <p>This supports the claim that:</p> <ul> <li>A single meaning θ₀ dominates in the human posterior belief,</li> <li>But alternate meanings retain small probability mass <d-cite key="TraxlerPickering2003SyntacticAmbiguity"></d-cite>.</li> </ul> <h3 id="context-dependence-and-disambiguation">Context-Dependence and Disambiguation</h3> <p>Work in contextual integration <d-cite key="MacDonaldPearlmutterSeidenberg1994LexicalAmbiguity"></d-cite> shows that humans update interpretations probabilistically as context accumulates, consistent with the multiplicative reduction in ambiguity predicted by ε-ambiguity theory, i.e. Proposition 1 in <d-cite key="Jiang2023LatentSpaceEmergent"></d-cite>.</p> <p>The ε-ambiguity model predicts (and proves) that when a listener receives multiple messages \((x_1, x_2, \dots, x_m )\) generated from the same θ:</p> \[\varepsilon_{\text{combined}} \approx \varepsilon(x_1)\varepsilon(x_2)\cdots\varepsilon(x_m).\] <p>This is aligned with psychological evidence that humans aggregate cues. This multiplicative decay of ε explains:</p> <ul> <li>why conversation repair is effective,</li> <li>why adding context sharpens meaning,</li> </ul> <p>Thus ε plays the role in controlling inference quality.</p> <h3 id="repair-mechanisms">Repair Mechanisms</h3> <p>Conversation analysis <d-cite key="SchegloffJeffersonSacks1977SelfCorrection"></d-cite> shows that misunderstandings occur at low but non-zero frequency, and repair strategies efficiently correct them—suggesting that ε(x) is generally small but salient.</p> <h3 id="cooperative-principle">Cooperative Principle</h3> <p>Grice’s cooperative principle <d-cite key="Grice1975LogicConversation"></d-cite> ensures that interpretation leans toward meanings that maximize communicative coherence, forcing</p> \[\Pr(\theta_0 \mid x) \gg \Pr(\theta\_{\text{alt}} \mid x),\] <p>even when multiple interpretations are technically possible.</p> <hr/> <h2 id="evidence-from-computational-linguistics"><strong>Evidence from Computational Linguistics</strong></h2> <h3 id="corpus-based-skew-of-meaning-distributions">Corpus-Based Skew of Meaning Distributions</h3> <p>Probabilistic models such as topic models, PCFGs, and neural parsers show extreme sparsity in the joint distribution of meanings and linguistic forms <d-cite key="ManningSchutze1999StatNLP"></d-cite>. For example:</p> <ul> <li>One parse typically has probability ≫ 0.9,</li> <li>Alternate parses share the remaining mass.</li> </ul> <p>This sparsity is exactly the structure assumed for ε-ambiguous languages.</p> <h3 id="behavior-of-large-language-models">Behavior of Large Language Models</h3> <p>LLMs themselves reveal ε-like behavior:</p> <h4 id="sensitivity-to-prompt-ambiguity">Sensitivity to Prompt Ambiguity</h4> <p>When prompts are under-specified, LLM outputs diverge, demonstrating non-zero ε(x). When prompts are clarified or expanded (e.g., chain-of-thought prompting), the model’s output variance collapses—interpretable as effective ε(x) decreasing multiplicatively with additional linguistic evidence <d-cite key="Wei2022ChainOfThought"></d-cite>.</p> <h4 id="convergence-under-contextual-redundancy">Convergence Under Contextual Redundancy</h4> <p>The theoretical models show that concatenating independent messages reduces ambiguity roughly like:</p> \[\varepsilon_{\text{combined}} \approx \prod_i \varepsilon(x_i).\] <p>This aligns with empirical improvements when LLMs receive:</p> <ul> <li>multiple examples in in-context learning <d-cite key="Brown2020FewShot"></d-cite>,</li> <li>multiple paraphrases of a query <d-cite key="Gao2021BetterFewShot"></d-cite>,</li> <li>deliberate step-by-step reasoning <d-cite key="Wang2022SelfConsistency"></d-cite>.</li> </ul> <h4 id="improved-performance-with-additional-cues">Improved Performance with Additional Cues</h4> <p>Work on instruction tuning <d-cite key="Ouyang2022InstructRLHF"></d-cite> shows that LLMs improve dramatically when intentions are expressed more explicitly; implicit or ambiguous instructions (large ε) yield errors.</p> <hr/> <h2 id="theoretical-justification-from-information-theory"><strong>Theoretical Justification from Information Theory</strong></h2> <p>The empirical observations in previous sections strongly suggest that natural languages align with the ε-ambiguity formalism. In this section, we present a more formal theoretical justification supporting the necessity of ε-ambiguity for any human language capable of large-scale communication, inference, and compositional generalization.</p> <h3 id="communication-under-uncertainty-requires-controlled-ambiguity">Communication Under Uncertainty Requires Controlled Ambiguity</h3> <p>Let θ denote a latent intention and x a surface linguistic signal. Human communication is characterized by:</p> <ol> <li> <p><strong>Inherent variability in production</strong><br/> Speakers do not produce perfectly deterministic signals for intentions.</p> \[H(x \mid \theta) &gt; 0.\] </li> <li> <p><strong>Redundancy and recoverability in comprehension</strong><br/> Listeners consistently recover the intended meaning despite variability:</p> \[\Pr(\theta_0 \mid x) \text{ is typically high}.\] </li> </ol> <p>This requires that the conditional distribution over intentions, q(θ ∣ x), be <strong>sharply peaked</strong> but not a delta distribution. Formally, this implies:</p> \[\Pr(\theta_0 \mid x) = 1 - \varepsilon(x),\] <p>where ε(x) captures the intrinsic noise or ambiguity.</p> <p>If ε(x) were zero:</p> <ul> <li>Language would be fully deterministic, like a programming language.</li> <li>Yet natural languages support ellipsis, metaphor, deixis, and underspecification—contradicting determinism.</li> </ul> <p>If ε(x) were large:</p> <ul> <li>Communication would be unreliable and human languages could not function as coordination tools <d-cite key="Lewis1969Convention"></d-cite>.</li> </ul> <p>Thus, human language must live in the intermediate regime:</p> \[0 &lt; \varepsilon(x) \ll 1.\] <p>This is precisely the definition of an ε-ambiguity language.</p> <h3 id="information-theoretic-justification">Information-Theoretic Justification</h3> <p>Given Shannon’s channel coding theorem, any efficient communication system must satisfy:</p> \[H(\theta \mid x) &gt; 0,\] <p>unless compressed messages carry arbitrarily large complexity.</p> <p>Natural languages are highly compressed representations of latent intentions. For compression to be efficient:</p> \[H(\theta \mid x) = \mathbb{E}[\varepsilon(x)] &gt; 0\] <p>must hold. But for communication to function at all:</p> \[H(\theta \mid x) \ll H(\theta)\] <p>must also hold.</p> <p>This yields:</p> \[0 &lt; \varepsilon(x) \ll 1.\] <p>Thus ε is not merely empirical—it is <strong>forced</strong> by fundamental information-theoretic constraints on communication between bounded agents.</p> <h3 id="ambiguity-as-a-structural-requirement-for-expressivity">Ambiguity as a Structural Requirement for Expressivity</h3> <p>A classic result from information theory states that, under bounded channel capacity, a communication code must balance:</p> <ul> <li>expressivity (large hypothesis space Θ),</li> <li>efficiency (short messages),</li> <li>robustness (recoverability under noise).</li> </ul> <p>Natural languages accomplish this by encoding intentions in <strong>probabilistic distributions over many correlated cues</strong> (syntax, semantics, prosody, discourse), with none individually deterministic. This “multi-cue redundancy” structure implies that:</p> \[q(x \mid \theta) \text{ is broad, but structured},\] <p>leading again to:</p> \[\Pr(\theta_0 \mid x) \approx 1 - \varepsilon(x)\] <p>for some small ε(x).</p> <p>The ε term mathematically captures the trade-off between:</p> <ul> <li>variability (which increases expressive richness), and</li> <li>stability (needed for mutual intelligibility).</li> </ul> <p>Thus <strong>ε-ambiguity is not an accident</strong> but a structural necessity for language to be both expressive and learnable.</p> <h3 id="ambiguity-is-necessary-for-compositionality">Ambiguity Is Necessary for Compositionality</h3> <p>A fully deterministic mapping from θ → x (ε = 0) would break compositionality in languages such as English:</p> <ul> <li>Metaphor, ellipsis, and deixis require listeners to infer missing structure.</li> <li>Rich morphology and syntax permit underspecified constructions that rely on inference rather than explicit specification.</li> </ul> <p>If ε were zero, all linguistic constructions would require exhaustive specification of intentions, leading to:</p> <ul> <li>infinitely long messages,</li> <li>no pragmatic inference,</li> <li>no flexible reference resolution.</li> </ul> <p>Conversely, if ε is small but non-zero, compositional structures can afford <strong>underspecification</strong>, because the listener’s inferential machinery resolves them with high probability.</p> <p>Thus ε &gt; 0 is a prerequisite for <strong>efficient and human-like generative grammar</strong>.</p> <hr/> <h2 id="bayesian-models-of-language-support-ε-ambiguity"><strong>Bayesian Models of Language Support ε-Ambiguity</strong></h2> <p>Probabilistic pragmatics <d-cite key="GoodmanFrank2016PragmaticInference"></d-cite> models the listener as:</p> \[\Pr(\theta \mid x) \propto \Pr(x \mid \theta)\Pr(\theta).\] <p>Empirically, these models consistently find:</p> <ul> <li>a dominant intention θ₀ that captures most posterior mass</li> <li>a long tail of alternative intentions with total mass ε(x)</li> </ul> <p>Thus:</p> \[\Pr(\theta_0 \mid x) = 1 - \varepsilon(x)\] <p>emerges naturally as a mathematical property of Bayesian interpretation under realistic priors and likelihoods.</p> <p>This shows that <strong>ε-ambiguity is a mathematically inevitable property of any communicative system interpreted via Bayesian reasoning</strong>, which includes both humans and LLMs.</p> <h2 id="conclusion"><strong>Conclusion</strong></h2> <p>Across linguistic, psycholinguistic, computational, and conversational evidence, natural languages display the following features:</p> <table> <thead> <tr> <th>Property</th> <th>Observed in Natural Languages</th> <th>Matches ε-Ambiguous Definition</th> </tr> </thead> <tbody> <tr> <td>Multiple interpretations possible</td> <td>✔</td> <td>ε(x) &gt; 0</td> </tr> <tr> <td>One interpretation strongly dominant</td> <td>✔</td> <td>Pr(θ₀ | x ) ≈ 1-ε(x)</td> </tr> <tr> <td>Ambiguity decreases with context</td> <td>✔</td> <td>ε(x₁x₂) ≈ ε(x₁)ε(x₂)</td> </tr> <tr> <td>Communication is highly reliable</td> <td>✔</td> <td>ε(x) generally small</td> </tr> <tr> <td>Occasional misinterpretation occurs</td> <td>✔</td> <td>ε(x) nonzero</td> </tr> </tbody> </table> <p>Natural languages therefore satisfy the fundamental requirements of ε-ambiguity languages: they encode meaning with probabilistic stability but not absolute determinacy.</p> <p>The ε-ambiguity model elegantly captures the essential structure of natural language semantics. Evidence from multiple disciplines demonstrates that natural languages exhibit:</p> <ul> <li>constrained ambiguity,</li> <li>dominant interpretations,</li> <li>context-driven reduction of uncertainty,</li> <li>non-zero but bounded error rates.</li> </ul> <p>At last, the following empirical facts also align closely with the ε-ambiguity framework and serve as the further evidence:</p> <ul> <li>Human communication requires nonzero uncertainty, but that uncertainty must be bounded ⟹ ε(x) &gt; 0 but small.</li> <li>Efficient, expressive languages require underspecification, which in turn requires ε(x) ≠ 0.</li> <li>Bayesian inference over meanings implies skewed but non-delta posterior distributions ⟹ the ε form.</li> <li>Multiple evidence aggregation reduces uncertainty multiplicatively, as predicted by the ε-ambiguity formalism.</li> <li>Shannon’s information theory forces 0&lt;ε≪1 in any efficient coding system.</li> <li>Psycholinguistic data shows humans behave exactly like ε-ambiguity decoders.</li> </ul> <p>Together, these points provide a rigorous foundation for treating natural languages as ε-ambiguity languages.</p>]]></content><author><name>Hui Jiang</name></author><category term="natural-language"/><category term="epsilon-ambiguity"/><summary type="html"><![CDATA[In this post, we examines the claim that natural languages are ε-ambiguity languages in the sense defined by the probabilistic theories of language and latent-intention inference in (Jiang, 2023). This article surveys linguistic, psycholinguistic, and computational evidence demonstrating that natural languages exhibit precisely this structure.]]></summary></entry><entry><title type="html">A Brief Introduction to Bitcoin</title><link href="https://incml.github.io/huijiang/blog/2025/Bitcoin/" rel="alternate" type="text/html" title="A Brief Introduction to Bitcoin"/><published>2025-08-14T00:00:00+00:00</published><updated>2025-08-14T00:00:00+00:00</updated><id>https://incml.github.io/huijiang/blog/2025/Bitcoin</id><content type="html" xml:base="https://incml.github.io/huijiang/blog/2025/Bitcoin/"><![CDATA[<p>In this post, we provide a concise introduction to the key technologies that underpin the Bitcoin network. This overview highlights several important implementations within Bitcoin and offers readers the foundational knowledge needed to understand how the network operates. More specifically, we cover how Bitcoin addresses are created, how wallets work, how the network generates and verifies transactions, how proof-of-work powers the blockchain, and new features like SegWit. Interested readers can refer to <d-cite key="Antonopoulos2014"></d-cite> <d-cite key="Rosenbaum2019"></d-cite><d-cite key="Ammous2018"></d-cite> for more technical details.</p> <h2 id="bitcoin-address"><strong>Bitcoin Address</strong></h2> <p>In the Bitcoin network, any bitcoin funds are essentially associated with a bitcoin address, which has a corresponding secret password. Bitcoin addresses, much like email addresses, are public information and can be viewed by anyone on the Bitcoin network. This means that anyone can track all transactions associated with a given address or send funds to it. However, in order to spend or transfer the bitcoins held at an address, the owner must use the secret password. Without this secret password, which must be kept secure and never shared, it is impossible to access or move the funds. From the perspective of cryptography, the bitcoin address and its corresponding password are equivalent to a pair of public and private keys. More precisely, we first draw a large random number (either 128-bit or 256-bit) as a private key. Using cryptographic techniques – most commonly elliptic curve cryptography (ECC) – a corresponding public key is then derived from this private key. As is well established, it is computationally infeasible to determine the private key from the public key. The diagram below illustrates the steps by which the Bitcoin network converts a public key into a Bitcoin address used on the network.</p> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/key-to-bitcoin-address.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. A diagram to show how to compute the bitcoin address based on a public key (Image adapted from <d-cite key="Antonopoulos2014"></d-cite>). </div> <p>In Figure 1, there are three key components that are explained in detail below:</p> <ol> <li> <p><strong>SHA256</strong>: a hash function that generates a 256-bit hush from any inputs.</p> </li> <li> <p><strong>RIPEMD-160</strong>: another hash function generates a 160-bit hash from any input. The use of double hashing – first applying SHA-256 and then RIPEMD-160 – enhances the security of the resulting hash by reducing the risk of collisions and preventing reversal from the hash back to the original input. At the same time, it shortens the length of a Bitcoin address from 256 bits to 160 bits, thereby greatly saving space in the shared Bitcoin ledger.</p> </li> <li> <p><strong>Base58check</strong>: this module serves two purposes: i) It applies an encoding method known as Base58 to represent data, which improves upon the commonly used Base64 encoding scheme. Unlike Base64, which relies on 64 characters, Base58 uses only 58 unambiguous characters, omitting the six confusing ones (0, O, l, I, +, /) to make it more human-friendly. ii) To provide additional protection against typos and transcription errors, a checksum mechanism is included. The checksum is generated by taking the first four bytes of the double SHA-256 hash of the input data. These bytes are then appended to the input data before applying Base58 encoding. See the following diagram for all steps in Base58check.</p> </li> </ol> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/base58check-encoding.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The following diagrams further explain the Base58 encoding and decoding processes between a public key hash and its corresponding Bitcoin address:</p> <ul> <li>Convert 25 bytes of a public key hash and its checksum ‘5f26….1e60’ into a Bitcoin address ‘19g6….RBPD’</li> </ul> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/key-to-address-example.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>Convert a bitcoin address back to its corresponding public key hash:</li> </ul> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/address-back-key-example.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Refer to <a href="https://incml.github.io/BTC-Explorer/btc-address-encoding.html">an interactive explorer</a> to learn various encoding methods used for bitcoin addresses.</p> <h2 id="wallets"><strong>Wallets</strong></h2> <p>If a Bitcoin user relies on the same address for all transactions, this can create security and privacy risks:</p> <ul> <li> <p>Since the private key is required to sign every transaction from that address, repeated use increases the likelihood of the private key being exposed.</p> </li> <li> <p>Because all addresses and transaction details are publicly visible on the network, consistently using a single address makes it easy to link all transactions to one user. Even if the user’s real-world identity is not directly revealed, this practice still represents a significant violation of privacy.</p> </li> </ul> <p>The solution to these problems is to use each Bitcoin address only once, requiring users to generate a new address for every transaction. As a result, a user may need to manage a large number of addresses, each with its own private key. To simplify this process, the concept of a <strong>wallet</strong> was introduced. A wallet allows a user to store and manage multiple Bitcoin addresses along with their corresponding private keys, all under the control of that user.</p> <p>In addition, a wallet can provide several other important functions for users. For instance, it can help initiate transactions from its stored addresses, using the securely held private keys to sign these transactions before broadcasting them to the network. A wallet can also continuously monitor the Bitcoin network to track the funds associated with all of its addresses. Moreover, it should offer reliable backup options or a secure recovery mechanism, enabling users to restore their private keys and addresses in case the wallet is lost or damaged.</p> <p>If all addresses and private keys in a wallet were generated independently at random, backing them up or restoring them would be extremely cumbersome. Hierarchical Deterministic (HD) wallets solve this problem by using a single random number, known as a seed, to generate as many private key–address pairs as needed. This approach allows the entire wallet to be backed up simply by recording the seed. If necessary, all addresses and private keys can be fully restored by applying the same generation method to that seed.</p> <h3 id="hierarchical-deterministic-hd-wallets"><strong>Hierarchical Deterministic (HD) Wallets</strong></h3> <p>The key idea behind HD wallets is to extend each private key into an extended private key (xprv), which consists of two components: the private key itself and a chain code. Likewise, each public key can be extended into an extended public key (xpub), containing the public key and a corresponding chain code. These chain codes are derived by applying HMAC-SHA512 to input data, producing a 512-bit hash that is then split into two parts: a 256-bit chain code and a 256-bit value used as the private key in an xprv or as the public key in an xpub. The chain codes enable the generation of additional xprvs or xpubs in a hierarchical tree structure, following a specified derivation path.</p> <ul> <li><strong>Deriving a master xprv and xpub</strong>: The wallet seed is processed with HMAC-SHA512 to produce a 512-bit hash. This hash is then split into two halves: the left 256 bits form the master private key, while the right 256 bits serve as the chain code. The chain code is subsequently used to derive additional private keys in the next steps.</li> </ul> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/xprv-xpub.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li><strong>Deriving hardened private keys for all child nodes</strong>: Each node in the tree is identified by a derivation path. For example, the derivation path ‘m/0/1’ indicates the second grandchild of the first child of the root, where m denotes the master private key. Child private keys are derived recursively in a top-down manner according to their respective derivation paths. The diagram below illustrates this process, showing how the ‘hardened’ private key m/1’ can be derived from its parent key m. Note that the corresponding public key for this node, denoted as M/1’, can be derived from the private key m/1’ using the standard cryptographic technique.</li> </ul> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/xprv-child.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>The whole process is shown as below:</li> </ul> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/xprv-whole-1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Alternatively, all child xpubs can be derived directly from their parent xpubs, without accessing the corresponding private keys (xprvs). Since this process never exposes private key information, it can safely be performed on a less secure server, such as a web server.</p> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/xprv-whole-2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>It is important to note that cryptographic analysis shows this alternative procedure—deriving a child extended public key (xpub) directly by adding it to the parent public key—is mathematically equivalent to the first method, in which the child extended private key (xprv) is first computed by adding it to the parent private key and then converted into the corresponding xpub. This alternative method is preferable because it does not expose the parent private keys.</p> <h3 id="mnemonic-sentences"><strong>Mnemonic sentences</strong></h3> <p>In an HD wallet, the long random seed can be encoded into a sequence of human-readable mnemonic words. Recording 12 or 24 simple words is far more convenient and user-friendly than writing down a lengthy and complex hexadecimal seed.</p> <ol> <li><strong>Encoding a seed into a mnemonic sentence</strong>: the following diagram shows an example to encode a 128-bit random seed into a mnemonic sentence of 12 English words.</li> </ol> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/seed-to-mnem-words.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ol> <li><strong>Decoding a mnemonic sentence into a seed</strong>: The following diagram shows the inverse process to convert a mnemonic sentence back to the seed. Here, the 4-bit checksum provides only a limited safeguard against typos and transcription errors when recording mnemonic words.</li> </ol> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/mnem-words-to-seed.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Refer to <a href="https://incml.github.io/BTC-Explorer/btc-wallet-generation.html">an interactive explorer</a> to learn the step-by-step technical details on how wallets manages bitcoin addresses.</p> <h2 id="transactions"><strong>Transactions</strong></h2> <p>On the Bitcoin network, users create transactions to transfer funds from one or more addresses to other addresses. Each transaction specifies the funds being spent (the inputs) and the destinations for those funds (the outputs). An input references a previous transaction by including its transaction ID (txid) – a double SHA-256 hash of the transaction – along with an index that identifies which output of the previous transaction is being used. An output specifies both the amount of bitcoin being sent and the recipient’s Bitcoin address where the funds will be stored.</p> <p>When a transaction is created, each input must be signed with the private key corresponding to the address, proving that the user legitimately controls the funds. The public key and the generated signature are then included in the transaction so that every node on the network can verify it. The public key is necessary because Bitcoin addresses are derived from public keys through hashing, and the hash function prevents reconstructing the public key directly from the address. Each signature is bound to the entire transaction, meaning the signing algorithm hashes all transaction data (excluding the signatures themselves). Consequently, any modification to the transaction after signing will invalidate the signature, making tampering immediately detectable.</p> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/transactions.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>To verify a transaction, two checks must be performed: (i) whether the provided public key corresponds to the source address, and (ii) whether the signature is valid. This verification process is carried out using small scripts, essentially miniature programs. The first component, called the signature script (or scriptSig), contains the signature and the public key. The second component, called the public key script (or scriptPubKey), contains the Bitcoin address – specifically the public key hash (PKH) – along with the instructions for how the verification should be executed.</p> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/verify-transaction.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Both parts are combined to execute on a stack-based virtual machine to verify the transaction.</p> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/stack-vm.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>To create new bitcoin, a special type of transaction called a <em>coinbase</em> transaction is used. Unlike regular transactions, it has no source addresses. Ultimately, all bitcoins in circulation can be traced back to one or more coinbase transactions.</p> <p>Refer to <a href="https://incml.github.io/BTC-Explorer/btc-transactions/index.html">an interactive explorer</a> to learn technical details on how bitcoin cores create/save transactions, and sign/verify transactions in bitcoin network.</p> <h2 id="the-blockchain"><strong>The Blockchain</strong></h2> <p>All new transactions on the network are grouped into a block roughly every 10 minutes. This block is then added to the chain of previous blocks, known as the blockchain. Each block contains cryptographic hash links that ensure the integrity of the entire chain, making the blockchain immutable.</p> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/blockchain.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Each block consists of a header and all the transactions included within it.</p> <p>A block header contains:</p> <ol> <li>The hash of the previous block’s header,</li> <li><strong>The Merkle root</strong>, representing the combined hash of all transactions in the block,</li> <li>A timestamp indicating when the block was created, and</li> <li>A hash value demonstrating <strong>proof-of-work</strong>.</li> </ol> <h3 id="the-merkle-tree"><strong>The Merkle Tree</strong>:</h3> <p>The Merkle root is the combined hash of all transactions in a block. It is calculated by creating a hierarchy of cryptographic hashes, namely a Merkle tree.</p> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/merkle-tree.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>From the above, it is clear that modifying any transaction within a block will alter the Merkle root, which is sufficient to validate the integrity of all transactions in that block. Another important use of the Merkle tree is that lightweight nodes can verify whether a specific transaction is included in a block without downloading the entire block. They can do this using only the block header together with a partial Merkle tree that contains the hashes along the path from the transaction up to the root in the Merkle tree.</p> <h3 id="proof-of-work"><strong>Proof of Work</strong></h3> <p>Miners on the network compete to create the next block to be added to the blockchain. The first miner to produce a valid proof-of-work earns the right to add its block to the chain. To generate a valid proof-of-work, the miner must find a block header hash (by varying the value of the nonce) that is less than or equal to the difficulty target specified by the network. The difficulty target is dynamically adjusted by the network to ensure that one new block is added to the blockchain in roughly 10 minutes.</p> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/proof-of-work.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <h2 id="segregated-witness"><strong>Segregated Witness</strong></h2> <p>Segregated Witness (SegWit) was a major technical upgrade to the Bitcoin protocol introduced in 2015. Its core idea is to separate signature data from transactions and place it into an external structure called the witness. As a result, a transaction’s ID (txid) is no longer tied to its signature data.</p> <figure> <picture> <img src="/huijiang/assets/img/2025-08-14-Bitcoin/segwit.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>SegWit brought several important improvements to Bitcoin. Most notably, it solved the transaction malleability problem, where a txid could be altered by modifying the signature portion of a transaction. In addition, SegWit allows more transactions to fit into each block, since individual transactions become smaller. It also reduces network bandwidth usage, as signature data does not always need to be transmitted across the network.</p> <p>SegWit was carefully designed to ensure both forward and backward compatibility:</p> <ol> <li> <p><strong>Forward compatibility</strong> – Nodes running older software can still validate SegWit transactions without rejecting them.</p> </li> <li> <p><strong>Backward compatibility</strong> – Transactions created by older software remain valid and function correctly with updated programs.</p> </li> </ol> <p>SegWit introduces a new address format encoded with <strong>Bech32</strong> instead of Base58Check, for example: <em>bc1qeqzjk7vume5wmrdgz5xyehh54cchdjag6jdmkj</em>. These SegWit addresses can be converted into what is known as a witness program, which is used during transaction verification to ensure compatibility so that both old and new software implementations can process them correctly. Furthermore, SegWit transactions can be included in the same block alongside legacy transactions. If a block contains any SegWit transactions, an additional Merkle tree is constructed from all witness data to produce a combined hash, known as the witness commitment. This witness commitment is stored in the coinbase transaction, allowing updated SegWit-enabled nodes to properly verify blocks that include SegWit transactions.</p> <p>Refer to <a href="https://incml.github.io/BTC-Explorer/btc-address-encoding.html">an interactive explorer</a> to learn how to generate Segwit addresses in details.</p>]]></content><author><name>Hui Jiang</name></author><category term="bitcoin"/><category term="crypto"/><summary type="html"><![CDATA[In this post, we provide a concise introduction to the key technologies that underpin the Bitcoin network. This overview highlights several important implementations within Bitcoin and offers readers the foundational knowledge needed to understand how the network operates.]]></summary></entry><entry><title type="html">A Deterministic View of Diffusion Models</title><link href="https://incml.github.io/huijiang/blog/2024/Deterministic-Diffusion-Models/" rel="alternate" type="text/html" title="A Deterministic View of Diffusion Models"/><published>2024-11-11T00:00:00+00:00</published><updated>2024-11-11T00:00:00+00:00</updated><id>https://incml.github.io/huijiang/blog/2024/Deterministic-Diffusion-Models</id><content type="html" xml:base="https://incml.github.io/huijiang/blog/2024/Deterministic-Diffusion-Models/"><![CDATA[<p>In recent years, diffusion models, a novel category of deep generative models <d-cite key="JiangMLF2021"></d-cite>, have made significant strides in producing high-quality, high-resolution images. Notable examples include GLIDE <d-cite key="nichol2022glidephotorealisticimagegeneration"></d-cite>, DALLE-2 <d-cite key="ramesh2022hierarchicaltextconditionalimagegeneration"></d-cite>, Imagen <d-cite key="saharia2022photorealistictexttoimagediffusionmodels"></d-cite>, and the fully open-source Stable Diffusion <d-cite key="rombach2022highresolutionimagesynthesislatent"></d-cite>. These models are traditionally developed based on the framework of Denoising Diffusion Probabilistic Models (DDPM) <d-cite key="ho2020denoisingdiffusionprobabilisticmodels"></d-cite>. In this probabilistic framework, the forward diffusion process is modeled as a Gaussian process with Markovian properties. Conversely, the backward denoising process employs neural networks to estimate the conditional distribution at each time step. The neural networks involved in the denoising process are trained to minimize the evidence lower bound (ELBO) <d-cite key="ho2020denoisingdiffusionprobabilisticmodels"></d-cite> <d-cite key="luo2022understandingdiffusionmodelsunified"></d-cite>, akin to the approach used in a Variational Autoencoder (VAE) <d-cite key="Kingma_2019"></d-cite>.</p> <p>In this post, we present a deterministic perspective on diffusion models. In this method, neural networks are constructed to function in the opposite way of a deterministic diffusion process that gradually deteriorates images over time. This training allows the neural networks to reconstruct or generate images by reversing the diffusion process without using any knowledge of stochastic process. This method simplifies the derivation of diffusion models, making the process more straightforward and comprehensible. Within this deterministic framework, diffusion models can be fully explained and derived from scratch using only a few straightforward mathematical equations, as shown in this concise, self-contained post. This approach requires only basic mathematical knowledge, eliminating the need for lengthy tutorials filled with hundreds of complex equations involving stochastic processes and probability distributions. <d-cite key="luo2022understandingdiffusionmodelsunified"></d-cite>.</p> <h2 id="deterministic-forward-diffusion-process"><strong>Deterministic Forward Diffusion Process</strong></h2> <figure> <picture> <img src="/huijiang/assets/img/2024-11-11-Deterministic-Diffusion-Models/deterministic-diffusion.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. A deterministic view of diffusion models in the forward diffusion process and the backward denoising transformation (Image adapted from <d-cite key="karagiannakos2022diffusionmodels"></d-cite>). </div> <p>In Figure 1, we illustrate a deterministic view of the typical diffusion process in diffusion models. Starting with any input clean image, denoted as \(\mathbf{x}_0\), the forward process incrementally corrupts the input image for each time step \(t=1,2,\cdots,T\). This corruption is achieved by progressively adding varying levels of Gaussian noises over time as</p> \[\mathbf{x}_t = \sqrt{\alpha_t } \mathbf{x}_{t-1} + \sqrt{1- \alpha_t } \, {\boldsymbol \epsilon}_t \;\;\; \forall t=1, 2, \cdots, T\] <p>adhering to a predefined noise schedule: \(\alpha_1, \alpha_2, \ldots, \alpha_T\), where the noise at each timestep is Gaussian, \({\boldsymbol \epsilon}_t \sim \mathcal{N}(0, \mathbf{I})\). This process gradually introduces more noise at each step, leading to a sequence of increasingly corrupted versions of the original image: \(\mathbf{x}_0 \to \mathbf{x}_1 \to \mathbf{x}_2 \to \cdots \to \mathbf{x}_T\). When \(T\) is large enough, the last image \(\mathbf{x}_T\) approaches to a Gaussian noise, i.e. \(\mathbf{x}_T \sim \mathcal{N}(0, \mathbf{I})\).</p> <p>Building on the so-called “nice property” outlined in <d-cite key="ho2020denoisingdiffusionprobabilisticmodels"></d-cite> <d-cite key="luo2022understandingdiffusionmodelsunified"></d-cite> <d-cite key="karagiannakos2022diffusionmodels"></d-cite>, the above diffusion process can be implemented much more efficiently. Rather than sampling a unique Gaussian noise at each time step, it is feasible to sample a single Gaussian noise, \(\boldsymbol{\epsilon} \sim \mathcal{N}(0, \mathbf{I})\), and employ the subsequent formula to efficiently generate all the corrupted samples in one go (along the left-to-right red dash arrow in Figure 1):</p> \[\begin{align} \mathbf{x}_t = f(\mathbf{x}_{0}, {\boldsymbol \epsilon}, t) = \sqrt{\bar{\alpha}_t} \mathbf{x}_{0} + \sqrt{1 - \bar{\alpha}_t} \, {\boldsymbol \epsilon} \;\;\; \forall t=1, 2, \cdots, T \label{eq-forward-deterministic} \end{align}\] <p>where \(\bar{\alpha}_t = \prod_{s=1}^t \alpha_s\) and we have \(\bar{\alpha}_t \to 0\) as \(t \to T\).</p> <p>In the diffusion process described in Eq. (\ref{eq-forward-deterministic}), once the noise \({\boldsymbol \epsilon}\) is sampled, it is treated as constant for the whole diffusion process. Consequently, the transformation from the clean image \(\mathbf{x}_{0}\) to noisy images \(\mathbf{x}_{t}\) at each time step \(t\) can be regarded as a deterministic mapping, characterized by the above function \(\mathbf{x}_t = f(\mathbf{x}_{0}, {\boldsymbol \epsilon}, t)\).</p> <p>As shown in Figure 2, clean images are gradually converted into pure noises in the above deterministic diffusion process as \(t\) goes from \(0\) to \(T\). The method in Eq.(\ref{eq-forward-deterministic}) streamlines the process, making the generation of corrupted samples more straightforward and less computationally demanding.</p> <figure> <picture> <img src="/huijiang/assets/img/2024-11-11-Deterministic-Diffusion-Models/deterministic-diffusion-process.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 2. The deterministic diffussion process of some images selected from the MNIST-Fashion dataset. </div> <h2 id="deterministic-backward-denoising-process"><strong>Deterministic Backward Denoising Process</strong></h2> <p>If the forward diffusion process is treated as deterministic, the corresponding deterministic mappings for the backward denoising process – transforming any noisy image \(\mathbf{x}_{t}\) back to the clean image \(\mathbf{x}_{0}\) – can also be derived.</p> <p>Here, let’s explore the relationship between \(\mathbf{x}_{t-1}\) and \(\mathbf{x}_t\) in the above diffusion process. This exploration will help us understand how consecutive stages in the diffusion process are linked by a deterministic function, which is essential for the subsequent deterministic denoising methods. In fact, it is possible to establish this deterministic function connecting two consecutive samples using two different approaches.</p> <p><strong>(1)</strong> In the first method, assuming the noise \({\boldsymbol \epsilon}\) is known, we can rearrange eq.(\ref{eq-forward-deterministic}) for the time step, \(t\), as follows:</p> \[\begin{align} \mathbf{x}_0 = \frac{1}{\sqrt{\bar{\alpha}_t}} \big[ \mathbf{x}_t - \sqrt{1 - \bar{\alpha}_t}\, {\boldsymbol \epsilon} \big] \label{eq-forwar-t} \end{align}\] <p>Furthermore, according to Eq.(\ref{eq-forward-deterministic}), for the previous time step \(t-1\), we have the following:</p> \[\begin{align} \mathbf{x}_{t-1} = \sqrt{\bar{\alpha}_{t-1}} \mathbf{x}_{0} + \sqrt{1 - \bar{\alpha}_{t-1}} \, {\boldsymbol \epsilon} \label{eq-forwar-t-1} \end{align}\] <p>We may substitue Eq.(\ref{eq-forwar-t}) into the above equation to derive the first relationship between any two adjacent samples as follows:</p> \[\begin{align} \begin{aligned} \mathbf{x}_{t-1} &amp;= \sqrt{\bar{\alpha}_{t-1}} \mathbf{x}_{0} + \sqrt{1 - \bar{\alpha}_{t-1}} \, {\boldsymbol \epsilon} \\ &amp;= \frac{1}{\sqrt{\alpha_t}} \big[ \mathbf{x}_t - \sqrt{1 - \bar{\alpha}_t}\, {\boldsymbol \epsilon} \big] + \sqrt{1 - \bar{\alpha}_{t-1}} \, {\boldsymbol \epsilon} \\ &amp;= \frac{1}{\sqrt{\alpha_t}} \Big[ \mathbf{x}_t - \big( \sqrt{1-\bar{\alpha}_t} - \sqrt{\alpha_t-\bar{\alpha}_t} \big) {\boldsymbol \epsilon} \Big] \end{aligned} \label{eq-consecutive-time-noise} \end{align}\] <p><strong>(2)</strong> Alternatively, assuming the original clean image \(\mathbf{x}_{0}\) is known, we can rearrange Eq.(\ref{eq-forward-deterministic}) as follows</p> \[{\boldsymbol \epsilon} = \frac{1}{\sqrt{1 - \bar{\alpha}_{t}}} \big[ \mathbf{x}_t - \sqrt{\bar{\alpha}_t} \mathbf{x}_{0}\big]\] <p>and substitute \({\boldsymbol \epsilon}\) into Eq.(\ref{eq-forwar-t-1}), we have</p> \[\begin{aligned} \mathbf{x}_{t-1} &amp;= \sqrt{\bar{\alpha}_{t-1}} \mathbf{x}_{0} + \sqrt{1 - \bar{\alpha}_{t-1}} \, {\boldsymbol \epsilon} \\ &amp;= \sqrt{\bar{\alpha}_{t-1}} \mathbf{x}_{0} + \frac{\sqrt{1 - \bar{\alpha}_{t-1}}}{\sqrt{1 - \bar{\alpha}_{t}}} \big[ \mathbf{x}_t - \sqrt{\bar{\alpha}_t} \mathbf{x}_{0}\big] \\ \end{aligned}\] <p>If we denote</p> \[\bar{\gamma}_t = \frac{1-\bar{\alpha}_{t-1}}{1-\bar{\alpha}_{t}}\] <p>we can simplify the above equation as follows:</p> \[\begin{align} \mathbf{x}_{t-1} = \sqrt{\bar{\gamma}_t} \, \mathbf{x}_t + \big( \sqrt{\bar{\alpha}_{t-1}} - \sqrt{\bar{\gamma}_t \bar{\alpha}_t} \big) \mathbf{x}_0 \label{eq-consecutive-time-x0} \end{align}\] <p>However, in practice, we cannot directly use Eq. (\ref{eq-consecutive-time-noise}) or Eq. (\ref{eq-consecutive-time-x0}) to derive \(\mathbf{x}_{t-1}\) from \(\mathbf{x}_t\) during the backward denoising process, as neither the noise \({\boldsymbol \epsilon}\) nor the original clean image ​\(\mathbf{x}_0\) is known. The key insight of diffusion models is that neural networks can be trained to approximate the inverse of the deterministic mapping function \(\mathbf{x}_t = f(\mathbf{x}_{0},\boldsymbol \epsilon, t)\) in the forward process. By leveraging this inverse mapping, the learned neural networks can help to estimate either the noise \({\boldsymbol \epsilon}\) or the original clean image ​\(\mathbf{x}_0\) from a noisy image \(\mathbf{x}_t\). There are two approaches for training neural networks, denoted as \({\boldsymbol \theta}\), to approximate this inverse function:</p> <ol> <li> <p>Approximate the inverse mapping from \(\mathbf{x}_t\) to the clean image \(\mathbf{x}_0\), i.e. \(\mathbf{\hat x}_0 = f^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t)\), allowing Eq. (\ref{eq-consecutive-time-x0}) to be applied for the backward denoising process. This inverse mapping is shown as the right-to-left red dash arrow in Figure 1.</p> </li> <li> <p>Approximate the inverse mapping from \(\mathbf{x}_t\) to the noise \({\boldsymbol \epsilon}\), i.e. \(\hat{\boldsymbol \epsilon} = g^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t)\), enabling the use of Eq. (\ref{eq-consecutive-time-noise}) for the backward denoising process.</p> </li> </ol> <p>Next, we will briefly explore these two different types of deterministic diffusion models.</p> <h2 id="the-deterministic-diffusion-models"><strong>The Deterministic Diffusion Models</strong></h2> <p>In the backward process, starting from a Gaussian noise</p> \[\mathbf{x}_T \sim \mathcal{N}(0, \mathbf{I})\] <p>we may gradually recover all corrupted images backwards one by one until we obtain the initial clean image:</p> \[\mathbf{x}_T \to \mathbf{x}_{T-1} \to \mathbf{x}_{T-2} \to \cdots \to \mathbf{x}_1 \to \mathbf{x}_0\] <p>Alternatively, at any time step \(t\), given the corrupted image \(\mathbf{x}_t\), we may also directly estimate the original clean image \(\mathbf{x}_0\) based on \(\mathbf{x}_t\). If the estimate is good enough, we can terminate the backward denoising process at an earlier stage; Otherwise, we further denoise one time step backwards, i.e. deriving \(\mathbf{x}_{t-1}\) from \(\mathbf{x}_t\). Based on \(\mathbf{x}_{t-1}\), we may derive a better estimate of the clean image \(\mathbf{x}_0\). This denoising process continues until we finally obtain a sufficiently good clean image \(\mathbf{x}_0\).</p> <p>In the above backward denoising process, to obtain a slightly cleaner image \(\mathbf{x}_{t-1}\) from \(\mathbf{x}_{t}\) using either Eq. (\ref{eq-consecutive-time-x0}) or Eq. (\ref{eq-consecutive-time-noise}), we have two options for training neural networks to approximate the inverse mapping, as previously discussed.</p> <h3 id="i-estimating-clean-image-mathbfx_0"><strong>I. Estimating clean image \(\mathbf{x}_0\)</strong></h3> <p>In this case, we construct a deep neural network \(\boldsymbol \theta\) to approximate the inverse function of the above diffusion mapping \(\mathbf{x}_t = f(\mathbf{x}_0, {\boldsymbol \epsilon}, t)\), denoted as</p> \[\mathbf{\hat x}_0 = f^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t)\] <p>which can recover a rough estimate of the clean image \(\hat{\mathbf{x}}_0\) from any \(\mathbf{x}_t\) (along the right-to-left red dash arrow in Figure 1). In this case, the neural network is learned by minimizing the following objective function over all training data in the training set \(\mathcal{D}\):</p> \[\begin{aligned} L_1({\boldsymbol \theta}) &amp;= \sum_{\mathbf{x}_0 \in \mathcal{D}} \sum_{t=1}^T \Big( f^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t) - \mathbf{x}_0\Big)^2 \\ &amp;= \sum_{\mathbf{x}_0 \in \mathcal{D}} \sum_{t=1}^T \Big( f^{-1}_{\boldsymbol \theta} \big(\sqrt{\bar{\alpha}_t} \mathbf{x}_{0} + \sqrt{1 - \bar{\alpha}_t} \, {\boldsymbol \epsilon}, t \big) - \mathbf{x}_0\Big)^2 \end{aligned}\] <p>Once the neural network \(\boldsymbol \theta\) has been trained, Eq. (\ref{eq-consecutive-time-x0}) can be used to perform the backward denoising process:</p> \[\begin{aligned} \mathbf{x}_{t-1} &amp;= \sqrt{\bar{\gamma}_t} \, \mathbf{x}_t + \big( \sqrt{\bar{\alpha}_{t-1}} - \sqrt{\bar{\gamma}_t \bar{\alpha}_t} \big) \hat{\mathbf{x}}_0 \\ &amp;= \sqrt{\bar{\gamma}_t} \, \mathbf{x}_t + \big( \sqrt{\bar{\alpha}_{t-1}} - \sqrt{\bar{\gamma}_t \bar{\alpha}_t} \big) f^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t) \\ \end{aligned}\] <p>The corresponding sampling process to generate a new image can be described as follows:</p> <ul> <li>sample a Gaussian noise \(\mathbf{x}_T \sim \mathcal{N}(0, \mathbf{I})\)</li> <li><strong>for</strong> \(t=T, T-1, \cdots, 1\): <ul> <li>use the trained neural network \({\boldsymbol \theta}\) to compute</li> </ul> \[\hat{\mathbf{x}}_0 = f^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t)\] <ul> <li><strong>if</strong> \(\hat{\mathbf{x}}_0\) is stable <strong>or</strong> \(t=1\), return \(\hat{\mathbf{x}}_0\)</li> <li><strong>else</strong> denoise one step backward as</li> </ul> </li> </ul> \[\mathbf{x}_{t-1} = \sqrt{\bar{\gamma}_t} \, \mathbf{x}_t + \big( \sqrt{\bar{\alpha}_{t-1}} - \sqrt{\bar{\gamma}_t \bar{\alpha}_t} \big) \hat{\mathbf{x}}_0\] <p>In Figure 3, we have shown some sampling results from the MNIST-Fashion dataset through the above sampling algorithm via building neural networks to estimate clean images.</p> <figure> <picture> <img src="/huijiang/assets/img/2024-11-11-Deterministic-Diffusion-Models/deterministic_denoising_via_cleanimage.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 3. Some sampling results are shown from the MNIST-Fashion dataset via building neural networks to estimate clean images. Every two lines represent one sampling example: the first line displays denoising samples at each timestep while the second line shows the estimated clean image at each timestep. </div> <h3 id="ii-estimating-noise-boldsymbol-epsilon"><strong>II. Estimating noise \({\boldsymbol \epsilon}\)</strong></h3> <p>In this case, we construct a deep neural network \(\boldsymbol \theta\) to approximate the inverse function via estimating the noise \({\boldsymbol \epsilon}\) from a corrupted image \(\mathbf{x}_t\) at each time step \(t\):</p> \[\hat{\boldsymbol \epsilon} = g^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t)\] <p>This neural network is learned by minimizing the following objective function over all training data:</p> \[\begin{aligned} L_2({\boldsymbol \theta}) &amp;= \sum_{\mathbf{x}_0 \in \mathcal{D}} \sum_{t=1}^T \Big( g^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t) - {\boldsymbol \epsilon}\Big)^2 \\ &amp;= \sum_{\mathbf{x}_0 \in \mathcal{D}} \sum_{t=1}^T \Big( g^{-1}_{\boldsymbol \theta} \big(\sqrt{\bar{\alpha}_t} \mathbf{x}_{0} + \sqrt{1 - \bar{\alpha}_t} \, {\boldsymbol \epsilon}, t \big) - {\boldsymbol \epsilon}\Big)^2 \end{aligned}\] <p>Once the neural network is learned, we can use Eq.(\ref{eq-consecutive-time-noise}) to derive an estimate of \(\mathbf{x}_{t-1}\) from \(\mathbf{x}_{t}\) as follows:</p> \[\begin{aligned} \mathbf{x}_{t-1} &amp;= \frac{1}{\sqrt{\alpha_t}} \Big[ \mathbf{x}_t - \big( \sqrt{1-\bar{\alpha}_t} - \sqrt{\alpha_t-\bar{\alpha}_t} \big) \hat{\boldsymbol \epsilon} \Big] \\ &amp;= \frac{1}{\sqrt{\alpha_t}} \Big[ \mathbf{x}_t - \big( \sqrt{1-\bar{\alpha}_t} - \sqrt{\alpha_t-\bar{\alpha}_t} \big) g^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t) \Big] \end{aligned}\] <p>Similarly, the corresponding sampling process to generate a new image can be described as follows:</p> <ul> <li> <p>sample a Gaussian noise \(\mathbf{x}_T \sim \mathcal{N}(0, \mathbf{I})\)</p> </li> <li> <p><strong>for</strong> \(t=T, T-1, \cdots, 1\):</p> <ul> <li>use the trained neural network \({\boldsymbol \theta}\) to compute:</li> </ul> \[\hat{\boldsymbol \epsilon} = g^{-1}_{\boldsymbol \theta} (\mathbf{x}_t, t)\] <ul> <li>estimate clean image as in Eq.(\ref{eq-forwar-t}):</li> </ul> \[\hat{\mathbf{x}}_0 = \frac{1}{\sqrt{\bar{\alpha}_t}} \big[ \mathbf{x}_t - \sqrt{1 - \bar{\alpha}_t}\, \hat{\boldsymbol \epsilon} \big]\] <ul> <li> <p><strong>if</strong> \(\hat{\mathbf{x}}_0\) is stable <strong>or</strong> \(t=1\), return \(\hat{\mathbf{x}}_0\)</p> </li> <li> <p><strong>else</strong> denoise one step backward as</p> </li> </ul> \[\mathbf{x}_{t-1} = \frac{1}{\sqrt{\alpha_t}} \Big[ \mathbf{x}_t - \big( \sqrt{1-\bar{\alpha}_t} - \sqrt{\alpha_t-\bar{\alpha}_t} \big) \hat{\boldsymbol \epsilon} \Big]\] </li> </ul> <p>In Figure 4, we have shown some sampling results from the MNIST-Fashion dataset via building neural networks to estimate noises through the above sampling algorithm.</p> <figure> <picture> <img src="/huijiang/assets/img/2024-11-11-Deterministic-Diffusion-Models/deterministic_denoising_via_noise.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 4. Some sampling results are shown from the MNIST-Fashion dataset via building neural networks to estimate noises. Every two lines represent one sampling example: the first line displays denoising samples at each timestep while the second line shows the estimated clean image at each timestep. </div> <h2 id="final-remarks"><strong>Final Remarks</strong></h2> <p>In recent years, diffusion models have become increasingly significant in computer vision, emerging as the dominant models for images and videos across a wide range of real-world applications. However, the traditional approach to understanding diffusion models presents a steep learning curve, as it requires advanced mathematical knowledge in areas such as probability density estimation, stochastic process, and differential equation <d-cite key="ho2020denoisingdiffusionprobabilisticmodels"></d-cite> <d-cite key="luo2022understandingdiffusionmodelsunified"></d-cite>. In this post, we have introduced a new deterministic perspective on diffusion models, enabling us to fully explain and derive them using only a few straightforward mathematical equations. Moreover, we have found that the two methods presented in this post are essentially equivalent to the Denoising Diffusion Implicit Models (DDIMs) method discussed in <d-cite key="song2022denoisingdiffusionimplicitmodels"></d-cite>. However, our methods are derived through a much simpler and more intuitive approach compared to the procedure outlined in <d-cite key="song2022denoisingdiffusionimplicitmodels"></d-cite>.</p>]]></content><author><name>Hui Jiang</name></author><category term="computer-vision"/><category term="machine-learming"/><category term="AI"/><summary type="html"><![CDATA[In this post, we present a deterministic perspective on diffusion models. In this approach, neural networks are trained as an inverse function of the deterministic diffusion mapping that progressively corrupts images at each time step. This method simplifies the derivation of diffusion models, enabling us to fully explain and derive them using only a few straightforward mathematical equations.]]></summary></entry><entry><title type="html">Understanding Transformers and GPT: An In-depth Overview</title><link href="https://incml.github.io/huijiang/blog/2023/Transformer-GPT/" rel="alternate" type="text/html" title="Understanding Transformers and GPT: An In-depth Overview"/><published>2023-03-05T00:00:00+00:00</published><updated>2023-03-05T00:00:00+00:00</updated><id>https://incml.github.io/huijiang/blog/2023/Transformer-GPT</id><content type="html" xml:base="https://incml.github.io/huijiang/blog/2023/Transformer-GPT/"><![CDATA[<p>Transformers <d-cite key="vaswani2023attentionneed"></d-cite> are a type of neural network architecture designed to transform a sequence of \(T\) input vectors,</p> \[\{ \mathbf{x}_1, \mathbf{x}_2, \cdots, \mathbf{x}_T \} \;\;\;\;\;(\mathbf{x}_i \in \mathbb{R}^d, \; \forall i=1,2,\cdots,T),\] <p>into an equal-length sequence of the so-called context-dependent output vectors:</p> \[\{ \mathbf{y}_1, \mathbf{y}_2, \cdots, \mathbf{y}_T \} \;\;\;\;\;(\mathbf{y}_i \in \mathbb{R}^h, \; \forall i=1,2,\cdots,T).\] <p>The output sequence in a transformer model is referred to as <em>context-dependent</em> because each output vector is influenced not only by the corresponding input vector but also by the context of the entire input sequence. Specifically, each output vector \(\mathbf{y}_i\) depends on all input vectors in the sequence, not just \(\mathbf{x}_i\) at the same position. As a result, each output vector can be viewed as a representation of not only the input vector at the same location but also its contextual information in the entire sequence.</p> <p>More importantly, transformers utilize a flexible attention mechanism that enables them to generate each output vector \(\mathbf{y}_i\) in a way that mainly relies on a certain number of the most relevant input vectors from anywhere in the input sequence, rather than just those input vectors near the position \(i\) . This ability to selectively attend to relevant information in the input sequence allows transformers to capture long-range dependencies and contextual information, making them a powerful tool for natural language processing and other sequential data tasks.</p> <h2 id="transformers-forward-pass"><strong>Transformers: Forward Pass</strong></h2> <p>Let’s pack all input vectors as a \(d \times T\) matrix, and all output vectors as an \(l \times T\) matrix, as follows:</p> \[\mathbf{X} = \bigg[ \mathbf{x}_1 \; \mathbf{x}_2 \; \cdots \; \mathbf{x}_T \bigg]_{d \times T}\] \[\mathbf{Y} = \bigg[ \mathbf{y}_1 \; \mathbf{y}_2 \; \cdots \; \mathbf{y}_T \bigg]_{h \times T}\] <p>In this way, a transformer can be viewed as a function \(\cal{T}\) that maps from \(\mathbf{X}\) to \(\mathbf{Y}\):</p> \[\cal{T}: \;\;\; \mathbf{X} \longrightarrow \mathbf{Y}\] <p>In the following, we will investigate all steps in the above mapping in a transformer.</p> <ul> <li><strong>Forward step 1:</strong> we first introduce three parameter matrices \(\mathbf{A}, \mathbf{B}, \mathbf{C} \in \mathbb{R}^{h \times d}\), which transform each input vector \(\mathbf{x}_i\) to generate the so-called <em>query</em> vector \(\mathbf{q}_i\), <em>key</em> vector \(\mathbf{k}_i\), and <em>value</em> vector \(\mathbf{v}_i\):</li> </ul> \[\mathbf{q}_i = \mathbf{A} \mathbf{x}_i, \; \mathbf{k}_i = \mathbf{B} \mathbf{x}_i, \; \mathbf{v}_i = \mathbf{C} \mathbf{x}_i \;\;\;\;(\forall i =1,2, \cdots,T)\] <p>When the <em>query</em>, <em>key</em>, and <em>value</em> vectors are all derived from a common input source, we refer to the transformer’s mechanism as performing <em>self-attention</em>. Conversely, if these vectors are derived from different sources, the mechanism is called <em>cross-attention</em>.</p> <p>The above operations can be combined as three matrix multiplications in the following:</p> \[\mathbf{Q} = \mathbf{A} \mathbf{X}, \;\; \mathbf{K} = \mathbf{B} \mathbf{X}, \;\; \mathbf{V} = \mathbf{C} \mathbf{X}\] <p>where \(\mathbf{Q}, \mathbf{K}, \mathbf{V} \in \mathbb{R}^{h \times T}\) are constructed by lining up the above vectors \(\mathbf{q}_i\), \(\mathbf{k}_i\) and \(\mathbf{v}_i\) column by column.</p> <ul> <li><strong>Forward step 2:</strong> use the above <em>query</em> and <em>key</em> vectors to compute all pair-wise attention between any two input vectors \(\mathbf{x}_i\) and \(\mathbf{x}_t\) (\(\forall i,t =1,2, \cdots, T\)) as follows:</li> </ul> \[c_{it} = \frac{\mathbf{q}_i^\intercal \mathbf{k}_t}{\sqrt{h}} \;\;\;\;\;\;\;\; (\forall i,t =1,2, \cdots, T)\] <p>Next, we normalize each \(c_{it}\) with respect to all \(i\) using the <em>softmax</em> function as follows:</p> \[a_{it} = \frac{e^{c_{it}}}{\sum_{j=1}^T \; e^{c_{jt}}} \;\;\;\;\;\;\;\;(\forall i,t =1,2, \cdots, T)\] <p>We can pack all operations in this step into the following matrix operation:</p> \[\mathcal{A} = \textrm{softmax}\big(\mathbf{Q}^\intercal \mathbf{K}/\sqrt{h} \big) \;\;\;\;\;\; (\mathcal{A} \in \mathbb{R}^{T \times T})\] <p>where the <em>softmax</em> operation is applied to the underlying matrix column-wise.</p> <ul> <li><strong>Forward step 3:</strong> use the above $a_{it}$ as the attention coeffificents to generate an context-dependent vector from all <em>value</em> vectors:</li> </ul> \[\mathbf{z}_t = \sum_{i=1}^T \; a_{it} \mathbf{v}_i \;\;\;\;\;\;\;\;(\forall t = 1,2,\cdots,T)\] <p>We can represent the above as the following matrix multiplication:</p> \[\mathbf{Z} = \mathbf{V} \mathcal{A} = \mathbf{V} \; \textrm{softmax}\big(\mathbf{Q}^\intercal \mathbf{K}/\sqrt{h} \big)\] <p>When transformers are employed as decoders to produce tokens, we typically utilize a form of attention known as causal attention. This attention mechanism ensures that each output vector is influenced solely by the input vectors that precede it, rather than any vectors that appear later in the sequence. In this case, we generate each $\mathbf{z}_t$ as</p> \[\mathbf{z}_t = \sum_{i=1}^t \; a_{it} \mathbf{v}_i \;\;\;\;\;\;\;\;(\forall t = 1,2,\cdots,T)\] <p>To compute causal attention in matrix form, an upper-triangular matrix is employed to mask the attention matrix $\mathcal{A}$. This masking ensures that the attention mechanism only attends to previous positions in the sequence, as represented by the upper-triangular elements of the attention matrix, while ignoring future positions represented by the lower-triangular elements.</p> <ul> <li><strong>Forward step 4:</strong> apply the layer normalization and one layer of fully-connected feedforward neural network to each $\mathbf{z}_t$ to generate the final output vector $\mathbf{y}_t$ as follows (note that residual connections <d-cite key="he2015deepresiduallearningimage"></d-cite> are introduced here to facilitate optimization during learning):</li> </ul> \[\bar{\mathbf{z}}_t = \mathbf{x}_t + \textrm{LN}_{\mathbf{X} + \gamma,\beta} \big( \mathbf{z}_t \big)\;\;\;\;\;\;\;(\forall t = 1,2,\cdots,T)\] \[\mathbf{y}_t = \bar{\mathbf{z}}_t + \textrm{feedforward} \big( \bar{\mathbf{z}}_t \big) = \bar{\mathbf{z}}_t + \mathbf{W}_2 \; \textrm{ReLU}( \mathbf{W}_1 \bar{\mathbf{z}}_t ) \;\;\;(\forall t = 1,2,\cdots,T)\] <p>where two more parameter matrices $\mathbf{W}_1 \in \mathbb{R}^{h’ \times h}$ and $\mathbf{W}_2 \in \mathbb{R}^{h \times h’}$ are introduced here. For convenience, we can use the following compact matrix form to represent all operations in this step:</p> \[\mathbf{Y} = \mathbf{X} + \textrm{LN}_{\gamma,\beta} \big( \mathbf{Z} \big) +\textrm{feedforward} \Big( \mathbf{X} + \textrm{LN}_{\gamma,\beta} \big( \mathbf{Z} \big)\Big).\] <p>In summary, we can illustrate all attention operations in the forward pass of a transformer using matrices as in Figure 1.</p> <figure> <picture> <img src="/huijiang/assets/img/2023-03-05-Transformer-GPT/transformer_attention.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Figure 1. An illustration of all attention operations (steps 1, 2 and 3) in a transformer. </div> <h2 id="transformers-backward-pass"><strong>Transformers: Backward Pass</strong></h2> <p>Let us now examine how to perform the backward pass to propagate errors in a transformer as well as how to compute the gradients of all transformer parameter matrices, specifically for all attention operations illustrated in Figure 1. For the backward pass of layer normalization and fully-connected feedforward layer, readers are directed to section 8.3.2 in reference <d-cite key="JiangMLF2021"></d-cite>.</p> <p>Assuming we possess error signals for the transformer outputs with respect to a particular objective function $F(\cdot)$, these signals are given as follows:</p> \[\mathbf{e}_t \overset{\Delta}{=} \frac{\partial F}{ \partial \mathbf{z}_t} \;\;\;\;\;\; (\forall t = 1, 2, \cdots, T)\] <p>Arrange them column by column as a matrix:</p> \[\mathbf{E} = \frac{\partial F}{\partial \mathbf{Z}} = \left[ \;\; \frac{\partial F}{ \partial \mathbf{z}_t} \;\; \right]_{h \times T}\] <p>Let’s break down all attention operations in a transformer in Figure 1 step by step backwards from the output towards input as follows:</p> <ul> <li><strong>Backward step 1:</strong> we have</li> </ul> \[\mathbf{z}_t = \sum_{i=1}^T a_{it} \, \mathbf{v}_i\;\;\;\;\;\;\;\;\;\; (\forall t = 1,2, \cdots, T)\] <p>According to the chain rule, we can compute</p> \[\frac{\partial F}{\partial a_{it} } = \frac{\partial F}{\partial \mathbf{z}_t } \frac{\partial \mathbf{z}_t}{ \partial a_{it}} = \mathbf{v}_i^\intercal \frac{\partial F}{\partial \mathbf{z}_t } \;\;\;\;\;(\forall i,t=1,2,\cdots,T)\] <p>Align all of these ($T^2$ terms in total) as a $T \times T$ matrix:</p> \[\left[ \;\; \frac{\partial F}{\partial a_{it}} \;\; \right]_{T \times T } = \bigg[ \;\; \mathbf{V}^\intercal \;\; \bigg]_{T\times h } \bigg[ \;\; \mathbf{E} \;\; \bigg]_{h \times T }\] <ul> <li><strong>Backward step 2:</strong> we normalize as $a_{it} = \frac{e^{c_{i}t}}{\sum_{j=1}^T e^{c_{jt}}} \;\;\;\;\;\; (\forall i,t = 1,2, \cdots, T)$.</li> </ul> <p>We denote</p> \[\mathbf{a}_t \overset{\Delta}{=} \big[ a_{1t} \, a_{2t} \, \cdots, a_{Tt} \big]^\intercal \;\;\;\;\; \textrm{and} \;\;\;\; \mathbf{c}_t \overset{\Delta}{=} \big[ c_{1t} \, c_{2t} \, \cdots, c_{Tt} \big]^\intercal\] \[\frac{\partial F}{\partial \mathbf{a}_t } \overset{\Delta}{=} \big[ \frac{\partial F}{\partial a_{1t}} \, \frac{\partial F}{\partial a_{2t}} \, \cdots \, \frac{\partial F}{\partial a_{Tt}} \big]^\intercal \;\;\;\;\; \textrm{and} \;\;\;\; \frac{\partial F}{\partial \mathbf{c}_t } \overset{\Delta}{=} \big[ \frac{\partial F}{\partial c_{1t}} \, \frac{\partial F}{\partial c_{2t}} \, \cdots \, \frac{\partial F}{\partial c_{Tt}} \big]^\intercal\] <p>According to Eq.(8.14) on page 180 in $[3]$, for any $t=1,2,\cdots,T$, we have</p> \[\frac{\partial F}{\partial \mathbf{c}_t } = \mathbf{J}_{\tiny sm}(t) \; \frac{\partial F}{\partial \mathbf{a}_t } \;\;\;\;\; (\forall t=1,2,\cdots,T)\] <p>with $\mathbf{J}_{\tiny sm}(t) = \textrm{diag} \big( \mathbf{a}_t \big) - \mathbf{a}_t \mathbf{a}_t^\intercal$. Furthermore, we use vector inner products to simplify the above matrix multiplications as follows:</p> \[\frac{\partial F}{\partial \mathbf{c}_t } = \mathbf{J}_{\tiny sm}(t) \; \frac{\partial F}{\partial \mathbf{a}_t } = \Big( \textrm{diag} \big( \mathbf{a}_t \big) - \mathbf{a}_t \mathbf{a}_t^\intercal \Big) \; \frac{\partial F}{\partial \mathbf{a}_t } = \mathbf{a}_t \odot \frac{\partial F}{\partial \mathbf{a}_t } - \big ( \mathbf{a}_t^\intercal \frac{\partial F}{\partial \mathbf{a}_t } \big)\mathbf{a}_t \;\;\;(\forall t=1,2,\cdots,T)\] <p>where $\odot$ indicates element-wise multiplication of two vectors. Next, we align the above results column by column for all $t=1,2,\cdots, T$ and use the notation $\otimes$ to indicate the batch of all $T$ above operations as follows:</p> \[\left[ \;\;\;\; \frac{\partial F}{\partial \mathbf{c}_t } \;\;\;\; \right]_{T \times T } = \mathcal{A} \otimes \left[ \;\;\;\; \frac{\partial F}{\partial \mathbf{a}_t} \;\;\;\; \right]_{T \times T }\] <p>It is worth noting that the aforementioned backward implementation can be applied directly to <em>causal attention</em> without any modifications.</p> <ul> <li><strong>Backward step 3:</strong> due to $c_{it} = \mathbf{q}_i^\intercal \mathbf{k}_t/\sqrt{h} \;\;\;\;\;\; (\forall i,t = 1,2,\cdots, T)$, we have</li> </ul> \[\frac{\partial F}{ \partial \mathbf{q}_i} = \sum_{t=1}^T \frac{\partial F}{\partial c_{it} } \frac{\partial c_{it}}{\partial \mathbf{q}_i} = \frac{1}{\sqrt{h}}\sum_{t=1}^T \frac{\partial F}{\partial c_{it} } \mathbf{k}_t\] <p>Align these vectors column by column as the following matrix format:</p> \[\bigg[ \;\; \frac{\partial F}{ \partial \mathbf{q}_i} \;\; \bigg]_{h \times T} = \frac{1}{\sqrt{h}} \bigg[ \;\; \mathbf{K} \;\; \bigg]_{h \times T} \left[ \;\; \frac{\partial F}{\partial \mathbf{c}_t } \;\; \right]^\intercal_{T \times T }\] <ul> <li><strong>Backward step 4:</strong> because of $\mathbf{q}_i = \mathbf{\mathbf{A}} \mathbf{x}_i$, we have</li> </ul> \[\frac{\partial F}{\partial \mathbf{A}} = \sum_{i=1}^T \frac{\partial F}{\partial \mathbf{q}_i} \frac{\partial \mathbf{q}_i}{\partial \mathbf{A}} = \sum_{i=1}^T \frac{\partial F}{\partial \mathbf{q}_i} \mathbf{x}_i^\intercal = \bigg[ \;\; \frac{\partial F}{ \partial \mathbf{q}_i} \;\; \bigg]_{h \times T} \bigg[ \;\; \mathbf{X}^\intercal \;\; \bigg]_{T\times d}\] <p>Putting all the above steps together, we have</p> \[\frac{\partial F}{\partial \mathbf{A}} = \frac{1}{\sqrt{h}}\bigg[ \;\; \mathbf{K} \;\; \bigg]_{h \times T} \Bigg( \mathcal{A} \otimes \Bigg( \bigg[ \;\; \mathbf{V}^\intercal \;\; \bigg]_{T\times h } \bigg[ \;\; \mathbf{E} \;\; \bigg]_{h \times T } \Bigg) \Bigg)^\intercal \bigg[ \;\; \mathbf{X}^\intercal \;\; \bigg]_{T\times d}\] \[= \frac{1}{\sqrt{h}} \mathbf{K} \bigg( \mathcal{A} \otimes \big( \mathbf{V}^\intercal \mathbf{E} \big) \bigg)^\intercal \mathbf{X}^\intercal\] <p>Similarly, we can derive</p> \[\frac{\partial F}{\partial \mathbf{B}} = \frac{1}{\sqrt{h}} \mathbf{Q} \bigg( \mathcal{A} \otimes \big( \mathbf{V}^\intercal \mathbf{E} \big) \bigg) \mathbf{X}^\intercal\] <p>Since we have</p> \[\mathbf{z}_t = \sum_{i=1}^T a_{it} \mathbf{v}_i\;\; (\forall t = 1,2, \cdots, T)\] <p>We compute</p> \[\frac{\partial F}{ \partial \mathbf{v}_i} = \sum_{t=1}^T a_{it} \frac{\partial F}{\partial \mathbf{z}_t}\] <p>Arrange them column by column into a matrix</p> \[\bigg[ \;\; \frac{\partial F}{ \partial \mathbf{v}_i} \;\;\bigg]_{h \times T} = \bigg[ \;\; \frac{\partial F}{ \partial \mathbf{z}_i} \;\;\bigg]_{h \times T} \bigg[ \;\; a_{it} \;\; \bigg]^\intercal_{T \times T} = \bigg[ \;\; \frac{\partial F}{ \partial \mathbf{z}_i} \;\;\bigg]_{h \times T} \mathcal{A}^\intercal = \mathbf{E} \mathcal{A}^\intercal\] <p>Since we have $\mathbf{v}_i = \mathbf{C} \mathbf{x}_i \;\; (\forall i=1,2,\cdots,T)$ , we compute</p> \[\frac{\partial F}{\partial \mathbf{C}} = \sum_{i=1}^T \frac{\partial F}{\partial \mathbf{v}_i} \frac{\partial \mathbf{v}_i}{\partial \mathbf{C}} = \sum_{i=1}^T \frac{\partial F}{\partial \mathbf{v}_i} \mathbf{x}_i^\intercal = \bigg[ \;\; \frac{\partial F}{ \partial \mathbf{v}_i} \;\; \bigg]_{h \times T} \bigg[ \;\; \mathbf{X}^\intercal \;\; \bigg]_{T\times d}\] <p>As a result, we have</p> \[\frac{\partial F}{\partial \mathbf{C }} = \mathbf{E} \mathcal{A}^\intercal \mathbf{X}^\intercal\] <p>Finally, we back-propagate the error signals from output to input. The input $\mathbf{X}$ affects the output through three different paths, i.e. $\mathbf{Q}$, $\mathbf{K}$ and $\mathbf{V}$. Therefore, we have</p> \[\frac{\partial F}{\partial \mathbf{X}} = \frac{\partial F}{\partial \mathbf{Q}} \frac{\partial \mathbf{Q}}{ \partial \mathbf{X}} + \frac{\partial F}{\partial \mathbf{K}} \frac{\partial \mathbf{K}}{ \partial \mathbf{X}} + \frac{\partial F}{\partial \mathbf{V}} \frac{\partial \mathbf{V}}{ \partial \mathbf{X}}\] \[= \bigg[ \;\; \mathbf{A}^\intercal \;\; \bigg]_{d\times h} \bigg[ \;\; \frac{\partial F}{ \partial \mathbf{q}_i} \;\; \bigg]_{h \times T} + \bigg[ \;\; \mathbf{B}^\intercal \;\; \bigg]_{d\times h} \bigg[ \;\; \frac{\partial F}{ \partial \mathbf{k}_i} \;\; \bigg]_{h \times T} + \bigg[ \;\; \mathbf{C}^\intercal \;\; \bigg]_{d\times l}\bigg[ \;\; \frac{\partial F}{ \partial \mathbf{v}_i} \;\; \bigg]_{h \times T}\] \[= \frac{1}{\sqrt{h}} \bigg( \mathbf{A}^\intercal \mathbf{K} \Big( \mathcal{A} \otimes \big( \mathbf{V}^\intercal \mathbf{E} \big) \Big)^\intercal + \mathbf{B}^\intercal \mathbf{Q} \Big( \mathcal{A} \otimes \big( \mathbf{V}^\intercal \mathbf{E} \big) \Big) \bigg) + \mathbf{C}^\intercal \mathbf{E} \mathcal{A}^\intercal\] <p>Finally, we summarize the above results using a more compact matrix representation. If we define the following $3h \times T$ matrix:</p> \[\mathbf{P} \overset{\Delta}{=} \begin{bmatrix} \frac{1}{\sqrt{h}} \mathbf{K} \bigg( \mathcal{A} \otimes \big( \mathbf{V}^\intercal \mathbf{E} \big) \bigg)^\intercal \\ \frac{1}{\sqrt{h}} \mathbf{Q} \bigg( \mathcal{A} \otimes \big( \mathbf{V}^\intercal \mathbf{E} \big) \bigg) \\ \mathbf{E} \mathcal{A}^\intercal \end{bmatrix}_{ 3h \times T}\] <p>we have</p> \[\begin{bmatrix} \frac{\partial F}{\partial \mathbf{A}} \\[0.1cm] \frac{\partial F}{\partial \mathbf{B}} \\[0.1cm] \frac{\partial F}{\partial \mathbf{C}} \end{bmatrix} = \mathbf{P} \, \mathbf{X}^\intercal \;\;\;\;\;\;\Big(\in \mathbb{R}^{3h \times d} \Big)\] \[\frac{\partial F}{\partial \mathbf{X}} = \begin{bmatrix} \mathbf{A} \\ \mathbf{B} \\ \mathbf{C} \end{bmatrix}^\intercal \, \mathbf{P} = \Big[ \mathbf{A}^\intercal \;\; \mathbf{B}^\intercal \;\; \mathbf{C}^\intercal \Big] \, \mathbf{P} \;\;\;\;\;\;\Big(\in \mathbb{R}^{d \times T} \Big)\] <p><em>Note: Refer to <a href="https://colab.research.google.com/drive/1-op--04cwJI2L8iPNIgNoPsJ3uPke8OB">a pytorch implementation at Colab</a> and its comparison with pytorch autograd (or a <a href="https://colab.research.google.com/drive/1WOdo0AuSn-lIzOxXLxTwDAKPJhzEvJ2e">JAX implementation at Colab</a>).</em></p> <h2 id="gpt-3"><strong>GPT-3</strong></h2> <p>The authors of <d-cite key="brown2020languagemodelsfewshotlearners"></d-cite> propose a deep multi-head transformer structure named <em>GPT-3</em> for processing text data. In GPT-3, the values of $d$, $h$, and $T$ are set to $d=12288$, $h=128$, and $T=2048$, respectively, while the vocabulary is composed of $50257$ distinct tokens.</p> <p>To begin, <em>GPT-3</em> employs a tokenizer to split any text sequence into a sequence of tokens (each token is a common word fragment). These tokens are then transformed into vectors of $d=12288$ dimensions using a word embedding matrix $\mathbf{W}_0 \in \mathbb{R}^{12288 \times 50257}$. Subsequently, the input sequence $\mathbf{X} \in \mathbb{R}^{12288 \times 2048}$ is transformed into $\mathbf{Y} \in \mathbb{R}^{12288 \times 2048}$ using 96 layers of multi-head transformer blocks. Each block is defined as follows:</p> <p><strong>(1)</strong> Each multi-head transformer in <em>GPT-3</em> uses $96$ heads:</p> \[\mathbf{A}^{(j)}, \mathbf{B}^{(j)}, \mathbf{C}^{(j)} \in \mathbb{R}^{128 \times 12288} \;\;\; (j=1,2, \cdots, 96)\] <p>which compute for all $j=1,2,\cdots,T$:</p> \[\mathbf{Z}^{(j)} \in \mathbb{R}^{ 128 \times 2048} = \big( \mathbf{C}^{(j)} \mathbf{X} \big) \; \textrm{softmax}\Big( \big(\mathbf{A}^{(j)} \mathbf{X} \big)^\intercal \big( \mathbf{B}^{(j)} \mathbf{X} \big)/\sqrt{128}\Big)\] <p><strong>(2)</strong> Concatenate the outputs from all heads:</p> \[\mathbf{Z} \in \mathbb{R}^{12288 \times 2048} = \mathbf{W}^o \textrm{concat}\big(\mathbf{Z}^{(1)}, \cdots, \mathbf{Z}^{(96)}\big)\] <p>where $\mathbf{W}^o \in \mathbb{R}^{12288\times12288}$.</p> <p><strong>(3)</strong> Apply layer normalization to each column of $\mathbf{Z}$: $\mathbf{z}_t \in \mathbb{R}^{12288} \; (\forall t=1,2,\cdots,2048)$ as</p> \[\bar{\mathbf{z}}_t = \mathbf{x}_t + \textrm{LN}_{\gamma,\beta} \big(\mathbf{z}_t \big)\] <p><strong>(4)</strong> Apply nonlinearity to each column as:</p> \[\mathbf{y}_t= \bar{\mathbf{z}}_t + \textrm{feedforward} \big( \bar{\mathbf{z}}_t\big) = \bar{\mathbf{z}}_t + \mathbf{W}_2 \textrm{ReLU} (\mathbf{W_1} \bar{\mathbf{z}}_t)\] <p>where $\mathbf{W}_1 \in \mathbb{R}^{49152 \times 12288}$, and $\mathbf{W}_2 \in \mathbb{R}^{12288 \times 49152}$.</p> <p>Based on these, we may calculate that the total number of parameters in <em>GTP-3</em> is about $175$ billions.</p> <p>During training, a sequence of training vectors</p> \[\{ \mathbf{x}_1, \mathbf{x}_2, \cdots, \mathbf{x}_{2048} \}\] <p>is fed into <em>GPT-3</em> as input $\mathbf{X} \in \mathbb{R}^{12288\times 2048}$. For each time step $t=1,2,\cdots,2047$, <em>GPT-3</em> is trained to predict the token at position $t+1$ based on all vectors appearing up to position $t$, i.e., ${\mathbf{x}_1, \cdots, \mathbf{x}_t}$.</p> <p>After its training, GPT-3 has the ability to create new sequences by using an input sequence as a prompt. To do this, the model calculates the probabilities of the possible next tokens that could follow the given prompt, and then selects a new token by randomly sampling from these probabilities. The selected token is then added to the end of the prompt, forming a new prompt. This process continues until the model generates a termination token.</p>]]></content><author><name>Hui Jiang</name></author><category term="machine-learming"/><category term="AI"/><category term="NLP"/><summary type="html"><![CDATA[In this post, we delve into the technical details of the widely used transformer architecture by deriving all formulas involved in its forward and backward passes step by step. By doing so, we can implement these passes ourselves and often achieve more efficient performance than using autograd methods. Additionally, we introduce the technical details on the construction of the popular GPT-3 model using the transformer architecture.]]></summary></entry><entry><title type="html">Machine Learning Fundamentals</title><link href="https://incml.github.io/huijiang/blog/2022/Machine-Learning-Fundamentals/" rel="alternate" type="text/html" title="Machine Learning Fundamentals"/><published>2022-01-21T01:00:00+00:00</published><updated>2022-01-21T01:00:00+00:00</updated><id>https://incml.github.io/huijiang/blog/2022/Machine-Learning-Fundamentals</id><content type="html" xml:base="https://incml.github.io/huijiang/blog/2022/Machine-Learning-Fundamentals/"><![CDATA[<div class="row"> <div class="col-sm-3 mt-3 mt-md-0"> <figure> <picture> <img src="/huijiang/assets/img/mlb-book-cover-small.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="example image" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>This post provides complementary materials for my recent textbook <a href="https://www.amazon.com/Machine-Learning-Fundamentals-Concise-Introduction/dp/1108940021"><strong>Machine Learning Fundamentals</strong></a> by <a href="https://incml.github.io/huijiang/"><em>Hui Jiang</em></a>, Cambridge University Press, 2021.</p> <h3 id="slides-per-chapter-detailed--contents-is-here">Slides per chapter (Detailed contents is <a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:detailedcontents.pdf">here</a>)</h3> <ul> <li><strong>Ch 1: Introduction</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch1_introduction.pdf">slides</a>)</li> <li><strong>Ch 2: Mathematical Foundation</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch2_mathematical_foundation.pdf">slides</a>)</li> <li><strong>Ch 3: Supervised Machine Learning (in a nutshell)</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch3_supervised_machine_learning.pdf">slides</a>)</li> <li><strong>Ch 4: Feature Extraction</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch4_feature_extraction.pdf">slides</a>)</li> <li><strong>Ch 5: Statistical Learning Theory</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch5_statistical_learning_theory.pdf">slides</a>)</li> <li><strong>Ch 6: Linear Models</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch6_linear_models.pdf">slides</a>)</li> <li><strong>Ch 7: Learning Discriminative Models in General</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch7_learning_discriminative_models.pdf">slides</a>)</li> <li><strong>Ch 8: Neural Networks</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch8_neural_networks.pdf">slides</a>)</li> <li><strong>Ch 9: Ensemble Learning</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch9_ensemble_learning.pdf">slides</a>)</li> <li><strong>Ch 10: Overview of Generative Models</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch10_overview_generative_models.pdf">slides</a>)</li> <li><strong>Ch 11: Unimodal Models</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch11_unimodal_models.pdf">slides</a>)</li> <li><strong>Ch 12: Mixture Models</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch12_mixture_models.pdf">slides</a>)</li> <li><strong>Ch 13: Entangled Models</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch13_entangled_models.pdf">slides</a>)</li> <li><strong>Ch 14: Bayesian Learning</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch14_bayesian_learning.pdf">slides</a>)</li> <li><strong>Ch 15: Graphical Models</strong> (<a href="https://wiki.eecs.yorku.ca/user/hj/_media/research:ch15_graphical_models.pdf">slides</a>)</li> </ul> <h3 id="lab-projects-using-jupyter-notebooks">Lab Projects (using Jupyter Notebooks)</h3> <ul> <li><strong>Lab 0</strong>: <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/pdf/Lab0_Preparation.pdf">Preparation</a> (available at <a href="https://colab.research.google.com/drive/1nB-uULhbpFTqlUOekEW3272eqKJg6qok?usp=sharing">Colab</a>, <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/ipynb/Lab0_Preparation.ipynb">ipynb</a>)</li> <li><strong>Lab 1</strong>: <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/pdf/Lab1_Data_Visualization.pdf">Data Visualization</a> (available at <a href="https://colab.research.google.com/drive/1zE2OrdJNkmzzn30CdDsj1y9qz585-afF?usp=sharing">Colab</a>, <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/ipynb/Lab1_Data_Visualization.ipynb">ipynb</a>)</li> <li><strong>Lab 2</strong>: <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/pdf/Lab2_Linear_Regression.pdf">Linear Regression</a> (available at <a href="https://colab.research.google.com/drive/1Ix04T8y0evnFnXWvHHWu0KfqsOA48hUg?usp=sharing">Colab</a>, <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/ipynb/Lab2_Linear_Regression.ipynb">ipynb</a>)</li> <li><strong>Lab 3</strong>: <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/pdf/Lab3_Logistic_Regression.pdf">Logistic Regression</a> (available at <a href="https://colab.research.google.com/drive/1qUi2qgwBMmHJb7Bx7ca_braGtptmzFOQ?usp=sharing">Colab</a>, <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/ipynb/Lab3_Logistic_Regression.ipynb">ipynb</a>)</li> <li><strong>Lab 4</strong>: <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/pdf/Lab4_Support_Vector_Machine.pdf">Support Vector Machines</a> (available at <a href="https://colab.research.google.com/drive/1vlN46Xhfv4ES8jFWWI0iTj3WHKjb0KMg?usp=sharing">Colab</a>, <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/ipynb/Lab4_Support_Vector_Machine.ipynb">ipynb</a>)</li> <li><strong>Lab 5</strong>: <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/pdf/Lab5_Fully_Connected_Neural_Networks.pdf">Fully-Connected Neural Networks</a> (available at <a href="https://colab.research.google.com/drive/1TY3fAAS18lyGrlEsvLhaYm6fIBP7CDgD?usp=sharing">Colab</a>, <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/ipynb/Lab5_Fully_Connected_Neural_Networks.ipynb">ipynb</a>)</li> <li><strong>Lab 6</strong>: <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/pdf/Lab6_Convolutional_Neural_Networks.pdf">Convolutional Neural Networks</a> (available at <a href="https://colab.research.google.com/drive/1Zkeujeoh4jNR9bQ67kfuIE7cV6KifZS2?usp=sharing">Colab</a>, <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/ipynb/Lab6_Convolutional_Neural_Networks.ipynb">ipynb</a>)</li> <li><strong>Lab 7</strong>: <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/pdf/Lab7_Transformers.pdf">Transformers</a> (available at <a href="https://colab.research.google.com/drive/1RvaBdX-KrgCNkVoo39UYqH1qzd-hAT5q">Colab</a>, <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/ipynb/Lab7_Transformers.ipynb">ipynb</a>)</li> <li><strong>Lab 8</strong>: <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/pdf/Lab8_Matrix_Factorization.pdf">Matrix Factorization</a> (available at <a href="https://colab.research.google.com/drive/1JaGmlkrC03URmnfkKtOakckP_ds20-WA">Colab</a>, <a href="https://github.com/iNCML/MachineLearningBook/blob/master/labs/ipynb/Lab8_Matrix_Factorization.ipynb">ipynb</a>)</li> <li><strong>Lab 9</strong>: Decision Trees, Random Forests and Boosted Trees</li> <li><strong>Lab 10</strong>: Gaussian Classifiers and Gaussian Mixture Models</li> </ul> <h3 id="citation-bibtex">Citation (bibtex):</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@book{Jiang-MLF-2021, 
  author = {Hui Jiang},
  title = {Machine Learning Fundamentals}, 
  publisher = {Cambridge University Press},
  year = {2021} 
}
</code></pre></div></div>]]></content><author><name></name></author><category term="machine-learming"/><category term="AI"/><summary type="html"><![CDATA[Supplementary materials for the book "Machine Learning Fundamentals", published by Cambridge University Press.]]></summary></entry></feed>